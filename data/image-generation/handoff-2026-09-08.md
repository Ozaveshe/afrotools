# Image director handoff — 2026-09-08

Base: preserved local commit `ba111af2`, branch `codex/image-library-20260908`. This base diverges from the fetched `origin/main` at `9b29eab0408eedd9442c98c2cabf567098da80ab`. Intake should use this scoped commit, not merge the entire historical branch. No main push or deployment was performed.

## Changed files and behavior

- Moved 60 reviewed WebP files from `assets/img/new/` to `assets/img/kitchen/`, with identical SHA-256 content. Three incoming files remain held: Malva Pudding (wrong dish), Dounguouri Soko (wrong dish), Sahrawi couscous (pixelated thumbnail).
- Refreshed 60 recipe image sections and six equivalent same-dish entries through the existing page generator, preserving release-owned analytics, canonical/hreflang and other content. Shared cuisine image data now resolves 312 recipe heroes, including six image aliases that do not duplicate binaries or change routes.
- Added nine reviewed existing illustrations to the tool card index and regenerated `tool-registry.min.js`. Two Lobola entries now select their own artwork instead of a generic family image.
- Repaired four missing image-path families across seven pages, including their known generator/manifest owners. Shared social fallback dimensions are 1200x630; recipe metadata uses measured image dimensions.
- Added an inventory of all 2,531 repository product images, reference evidence, dimensions, hashes, duplicate links and review assignments. 1,735 have reference/pipeline evidence; 792 remain unassigned for review, three are held, and one additional unassigned exact duplicate is in duplicate review. Eight exact duplicates overall are retained. Seventy images have explicit visual review for text-free reuse. 1,187 assets have reference evidence across locales; this is not proof that every such image is text-free.
- Prepared exactly 200 unique prompts: 98 recipe heroes, one tool card and 101 serving details. CSV, JSON and Markdown include routes, filenames, prompts, dimensions, priority, alt text and serving-photo references.

## Validation

Passed:

- `node tests/image-library.test.js`: original file hashes, 60 image placements, six image aliases, metadata preservation, source/minified tool index parity, 200 unique existing-route gaps, no missing static image-reference candidates.
- `node scripts/build-tool-image-index.js --check`
- `node scripts/repair-image-references.js --check`
- `git diff --check`
- Local Chromium: 60 image HTTP checks and ten desktop/390px recipe render/decode checks, no horizontal overflow or page errors. See `browser-proof.json`. External requests were blocked; this is local browser proof.
- `npm run check-links`: 141,479 links across 11,730 pages, no broken internal links.
- `npm run audit`: repository registry audit passed.
- `npm run seo:report`: no missing canonical/title/description or remaining hreflang violations; existing auto-fix suggestions were not applied wholesale.
- `npm run security:scan`
- `npm run build:deploy`; incidental generated churn was restored to the staged image-only scope. After final alias/registry refinements, `node scripts/build-dist.js` and `npm run audit:dist` passed again. Final artifact checks confirm all nine new artwork selections and six aliases.

Not verified: live provider/database usage, production rendering, image licensing, exhaustive dynamic image reachability, or visual/text review of every older raster asset. Unassigned images are retained for review, not declared unused or forced onto unrelated pages. No raw personal data or new network processing was introduced. No route, analytics event, or calculation behavior was intentionally changed.

## Dashboard and daily workflow

The separate task `01a07f9a-5063-7ad0-a52b-85faed98f837` owns `mc-7a2f9x.html` and its operator dashboard. It consumes these image outputs into its excluded admin snapshot. Its source and assets remain a separate release intake.

Daily automation `afrotools-daily-image-batch` checks at 09:00 and stays quiet without new deliveries/actionable changes. Pending generation requests are retained until delivery; generation itself is not automated. The older paused blog-specific queue was left unchanged.

Rollback: revert this scoped commit. The 60 asset moves are Git renames, and the original content is preserved in history.
