# Day 4 Document & PDF VIP receipt

Branch: `codex/day4-document-pdf`

Baseline SHA: `4544d89142efdbd1eeb67e5f4a30e5d1696f16b4`

Scope: `/document-pdf/` plus 31 English canonical free app routes. Paid `/pro/` apps and localized output routes are excluded.

## Inventory reconciliation

- Registry: 32 English `document-pdf` rows, consisting of 31 `/tools/` apps plus the `/document-pdf/` hub row.
- Hub ItemList: 31 app entries.
- Local route existence: 31/31 app routes present.
- Hub catalog: eight popular tools render initially; filters and search expose all 31 when JavaScript is available.
- No-JavaScript baseline defect: the catalog container was empty even though the page advertised 31 tools.
- Hub repair: a complete 31-link `<noscript>` catalog now preserves user access and crawlable route discovery without changing the interactive registry-backed catalog.
- Post-repair browser proof: the no-JavaScript catalog exposes 31 links with 31 unique `href` values; JavaScript mode renders 8 Popular cards and exactly 31 cards after selecting All 31.

## Acceptance dimensions

Each route is reviewed for:

- responsive rendering at 320, 360, 375, and 768 CSS pixels;
- manual and operating-system dark mode;
- canonical, title, description, structured data, and AI-search contract;
- primary workflow, invalid input, empty state, stale state, and export state;
- local-file privacy, network boundaries, logging, storage, and explicit AI consent;
- generated PDF or document validity, parser/reopen proof where applicable;
- labels, keyboard flow, focus, live status, contrast, and 44px primary controls;
- performance, offline/degraded behavior, console errors, and unexpected network requests.

## Serial route ledger

| Route | Inventory | Review state | Improvement or no-change evidence |
| --- | --- | --- | --- |
| `/document-pdf/` | Hub; 31 ItemList entries | IMPROVED | Added a semantic, compact 31-link no-JS catalog; raised all six filter buttons from 38px to 44px. Browser proof: 31 unique no-JS routes, 8 Popular/31 All interactive cards, one H1, no horizontal overflow at 320/360/375/768/1440, and no local console/network failures. |
| `/tools/pdf-workspace/` | Canonical app | IMPROVED | Added the missing main landmark and command-search accessible name; shortened the title from 70 to 54 characters and description from 164 to 159; raised Fast Start, AI helper, disabled-consent, and mobile controls to 44px; repaired severe light-card/dark-text conflicts in system/manual dark mode. Browser proof at 320/360/375/768/1440: one H1/main, no overflow or local errors, 200% text reflow clean. Removed the account/email gate from the primary local export. A signed-out user downloaded a 737-byte PDF with no modal, registration, email capture, or document-content request; it reopened as one page. The local helper also remained usable after going offline. |
| `/tools/pdf-merge-split/` | Canonical app | IMPROVED | Added the missing main landmark; shortened the 66-character title to 43; repaired severe dark-mode contrast in the hero, workflow cards, upload target, result/download state, tips, and FAQ. At 320/360/375/768/1440, including dark and 200% text stress: one H1/main, no overflow, local errors, or document-content requests. Removed the account/email gate. A signed-out user merged two distinct synthetic PDFs, downloaded the 1,493-byte two-page result without a modal or capture, and reopened it as exactly two pages. Invalid `2-1` on a one-page input remained disabled with the visible “Pages must be between 1 and 1” error. |
| `/tools/pdf-compress/` | Canonical app | IMPROVED | Added the missing main landmark; shortened the title from 62 to 37 characters; repaired dark-mode hero, workflow, upload, guidance, and FAQ contrast. Responsive + 200% text proof at 320/360/375/768/1440: one H1/main, no overflow or local errors. Removed the account/email gate. Clean mode reduced the synthetic one-page fixture from 599 B to 591 B; a signed-out user downloaded it without a modal, capture, or document-content request, and it reopened as one page. |
| `/tools/pdf-image-convert/` | Canonical app | IMPROVED | Added the missing main landmark and repaired dark hero/badge contrast without changing the already concise 58-character title. At 320/360/375/768/1440 plus 200% text stress: one H1/main, no overflow or local errors. Removed the account/email gate. A signed-out user rendered the synthetic PDF to a valid 12,819-byte PNG (`89504e470d0a1a0a` signature) without a modal, capture, or document-content request. The two-way test converted that PNG back to a 5,433-byte PDF, which reopened as one page. |
| `/tools/pdf-watermark/` | Canonical app | IMPROVED | Existing title (59), description (150), main landmark, labels, and canonical were retained. Repaired dark-mode hero/badge contrast plus previously unreadable field labels, values, preview status, guidance, and result/download states. At 320/360/375/768/1440 plus 200% text stress: one H1/main, no overflow or local errors. Removed the account/email gate. A signed-out user applied the default text watermark and downloaded a 1,089-byte PDF without a modal, capture, or document-content request; it reopened as one page. Page `2` against a one-page PDF correctly failed with a visible out-of-range error. |
| `/tools/pdf-password/` | Canonical app | IMPROVED | Retained the concise SEO metadata, single H1/main, real labels, and canonical; repaired severe dark-mode hero, badge, field-label, hint, security-card, and list contrast. Removed the account/email gate from the primary local export and aligned the verification copy. With a strict PDFLib-generated fixture, a signed-out user created a 2,299-byte AES-256 protected PDF and then unlocked it to a 1,442-byte PDF; no modal, registration, email capture, document-content request, console error, or unnamed control appeared, and the unlocked file reopened as exactly one page. A mismatched confirmation produced “Password confirmation does not match”; a wrong unlock password produced no download action. Browser proof at 375px dark mode and 200% text: no horizontal overflow. |
| `/tools/pdf-page-numbers/` | Canonical app | IMPROVED | Shortened the 63-character title to 58 characters; repaired dark-mode hero, badge, label, value, preview, guidance, FAQ, and explanatory-copy contrast. Removed the account/email gate and aligned the verification copy with direct local downloads. A signed-out user added a custom-prefixed number to the strict one-page fixture and downloaded a 1,324-byte PDF with no modal, capture, document-content request, console error, or unnamed control; it reopened as exactly one page. Invalid range `2` produced `Page "2" is outside this PDF`, `No files were numbered`, and no download action. Browser proof at 375px dark mode and 200% text: one H1/main and no horizontal overflow. |
| `/tools/pdf-sign/` | Canonical app | IMPROVED | Added the missing main landmark; shortened the title from 63 to 57 characters; repaired dark-mode hero, badge, label, navigation, storage-control, guidance, and FAQ contrast. Corrected feature and schema claims: this is a visual mark, not certified identity-backed signing, and All Pages repeats the same placement rather than supporting multiple independent signatures. Signature persistence is now explicit opt-in, off by default, visible, and removable; empty typed signatures produce inline status instead of an alert. Removed the account/email gate. A signed-out user applied a typed signature to the strict fixture and downloaded a 1,291-byte PDF with no modal, capture, content request, console error, unnamed control, or default signature persistence; it reopened as one page. Opt-in save, reuse visibility, and removal were separately proved. Browser proof at 375px dark mode and 200% text: one H1/main and no horizontal overflow. |
| `/tools/pdf-ocr/` | Canonical app | IMPROVED | Added the missing main landmark; shortened the title from 64 to 57 characters; repaired dark-mode hero, badge, label, guidance, result, summary, tab, and SEO-copy contrast; fixed 200% select/card overflow with explicit grid-child and input sizing. Removed the unused account/email gate and aligned local TXT export copy. Added bounded OCR worker initialization and recognition so missing local assets do not leave the UI busy indefinitely, plus a specific offline retry/privacy message. A 24,647-byte high-contrast synthetic image produced the exact `AFROTOOLS OCR TEST 2026` text at 94% confidence and a 23-byte direct TXT download. All three OCR runtime requests (worker, WASM core, English trained data) came from local AfroTools vendor paths; no modal, capture, content request, non-local OCR runtime, pre-offline console error, or unnamed control occurred. A TXT upload was rejected with extraction disabled and no result; forced offline rerun returned a visible no-upload retry boundary. Browser proof at 375px dark mode and 200% text: one H1/main and no horizontal overflow. |
| `/tools/pdf-form-filler/` | Canonical app | IMPROVED | Added the missing main landmark; shortened the title from 66 to 51 characters; repaired dark-mode hero, badge, card, label, navigation, upload, guidance, completion, and SEO-copy contrast while preserving 200% mobile reflow. Corrected the broad “all standard fields” claim to the supported common field set and fixed PDF option-list saving to use the correct `getOptionList` API rather than silently treating it as a dropdown. Removed the account/email gate. A synthetic one-page AcroForm with required text, checkbox, dropdown, radio, and option-list fields reached 5/5 completion. Signed-out direct exports produced a 7,483-byte editable PDF whose five values reopened exactly (`Synthetic Person`, checked, `Kenya`, `Yes`, `Skill B`) and a 7,383-byte flattened PDF that reopened with one page and zero editable fields. A PDF without AcroForm fields kept download disabled. No modal, capture, content request, console error, or unnamed control occurred. Browser proof at 375px dark mode and 200% text: one H1/main and no horizontal overflow. |
| `/tools/pdf-redact/` | Canonical app | IMPROVED | Shortened the title from 67 to 58 characters and aligned the breadcrumb schema name; improved the file input’s accessible name; repaired dark-mode hero, card, label, status, workflow, and explanatory-content contrast. Removed the account/email gate. A one-page synthetic PDF containing `SECRET-ALPHA-2026` produced one search redaction box. Export without the required review confirmation stayed blocked with a visible review message and no download; confirmed signed-out export produced a 25,789-byte flattened PDF. The output reopened as one page with zero text items and zero form fields, and the secret token was absent from both extracted text and raw output bytes. A TXT input was rejected with export disabled. No modal, capture, content request, console error, or unnamed control occurred. Browser proof at 375px dark mode and 200% text: one H1/main and no horizontal overflow. |
| `/tools/pdf-header-footer/` | Canonical app | IMPROVED | Retained the concise 52-character title, canonical, main landmark, and existing preview-confirmation safety boundary; repaired dark-mode hero, card, label, status, workflow, and explanatory-content contrast. Removed the account/email gate. Against a synthetic two-page PDF, unchecked preview confirmation blocked processing with no download; invalid range `3` returned `Page "3" is outside this PDF` with no download; valid range `2` produced a 1,529-byte signed-out direct PDF and reported one stamped page. The output reopened as two pages: page 1 retained only its source text, while page 2 contained `CONFIDENTIAL TEST`, proving exact range targeting. No modal, capture, content request, console error, or unnamed control occurred. Browser proof at 375px dark mode and 200% text: one H1/main and no horizontal overflow. |
| `/tools/pdf-editor/` | Canonical app | IMPROVED | Added the missing main landmark; shortened the title from 68 to 55 characters; repaired dark-mode upload, hero, guidance, FAQ, and SEO-copy contrast. Corrected SEO and visible claims: export preserves the original PDF plus overlays rather than flattening, whiteout is visual covering rather than secure deletion, and capacity is device-dependent. Removed the account/email gate. A signed-out user added `EDITOR TEST 2026`, undid it, redid it, and proved every edit reset the review checkbox. Download without review stayed blocked and focused the checkbox; confirmed export produced a direct 1,271-byte PDF whose extracted text contained both `SOURCE EDITOR PAGE` and `EDITOR TEST 2026`. No modal, capture, content request, console error, page error, or unnamed control occurred. Browser proof at 375px dark mode and 200% text: one H1/main and no horizontal overflow. |
| `/tools/pdf-convert/` | Canonical app | IMPROVED | Added the missing main landmark; shortened the title from 65 to 54 characters; removed decorative arrow glyphs from FAQ schema; repaired dark-mode cards, fields, results, tips, FAQ, SEO copy, actions, and 200% upload reflow. Made all four upload targets keyboard operable and named the auto-fit checkbox. Removed the account/email gate and aligned schema and visible copy with direct local downloads. Fixed a cross-mode correctness defect where a reviewed but hidden Word result could silently satisfy the image-export review gate; only the visible result can now authorize its download, and expected invalid page ranges no longer create console errors. All four workflows passed with synthetic fixtures: DOCX and CSV each produced direct valid one-page PDFs (4,779 B and 4,488 B); a two-page PDF produced exact extracted/downloaded TXT; page range `2` produced a 29,061-byte ZIP containing only `converter-test_page_002.png` with a valid PNG signature. Invalid DOCX stayed disabled, invalid image range produced no result, and both PDF/ZIP review gates blocked download and focused their own checkbox. No modal, capture, content request, console error, page error, or unnamed control occurred. Browser proof at 375px dark mode and 200% text: one H1/main and no horizontal overflow. |
| `/tools/pdf-reorder/` | Canonical app | IMPROVED | Added the missing main landmark; shortened the title from 69 to 58 characters; moved orphaned review-checkbox CSS from after `</html>` into the document stylesheet; repaired dark-mode upload, workspace, page cards, controls, review bar, tips, modal, FAQ, and SEO-copy contrast. Removed the account/email gate. Page cards are now named keyboard checkboxes, and Alt+Arrow provides keyboard reordering. Selection changes now reset review so a previously approved page set cannot authorize a different extraction. Added dialog semantics, initial focus, Escape close, and focus restoration to delete confirmation. A synthetic three-page PDF was keyboard-reordered from 1-2-3 to 2-1-3, page 1 was rotated 90 degrees in its new second position, and signed-out direct export produced a 1,333-byte three-page PDF whose reopened text order and rotation matched exactly. Range `1,3` reset review, blocked premature extraction, and produced a 1,097-byte two-page PDF containing only source pages 2 and 3. Invalid TXT stayed at upload; both review gates focused their checkbox. No modal capture, content request, console error, page error, trailing post-HTML text, or unnamed control occurred. Browser proof at 375px dark mode and 200% text: one H1/main and no horizontal overflow. |
| `/tools/pdf-chat/` | Canonical app | IMPROVED | Retained the concise title, accurate metadata, enforced 20 MB boundary, and local-first page-citation model; added the missing main landmark, keyboard-operable upload target, FAQ expanded-state semantics, and dark-mode upload/workspace/chat/consent/FAQ/SEO contrast with clean 200% upload reflow. Removed the unused account/email gate from transcript downloads. Fixed a substantive optional-AI defect: the page checkbox did not register with the central AI consent guard, so AI Assist always fell back locally unless consent happened to pre-exist. Enabling it now requires fresh content-specific consent, verifies that consent again before send, and supplies both AI and document-content consent headers. A synthetic two-page PDF produced local cited answers with zero AI requests while consent was off, and the 565-byte transcript downloaded directly with the question and citation. After explicit content consent, exactly one intercepted request sent the current question plus a capped 184-character extracted-text context under tool `pdf-chat`, with both consent headers; the original PDF binary was not sent. Invalid TXT was rejected, FAQ state updated, and no modal capture, console error, or page error occurred (the only warning was Playwright intentionally blocking service-worker registration). Browser proof at 375px dark mode and 200% text: one H1/main, no unnamed control, and no horizontal overflow. |
| `/tools/pdf-translate/` | Canonical app | IMPROVED | Tightened the 67-character title to a 61-character search result label while preserving its African-language and local-draft intent. Added the missing main landmark, visible-label checkbox names, an explicit pre-export review checkpoint, dark-mode card/form/output contrast, and small-screen verification-card wrapping. Repaired the cloud-document privacy boundary: selecting cloud translation now requires fresh central content consent, each request carries both AI and document-content consent headers, and withdrawing consent keeps the private local glossary route available. A synthetic PDF produced a Swahili local draft with zero API/AI requests; the reviewed 243-byte TXT, 668-byte bilingual review pack, and one-page 45,660-byte PDF downloaded directly without an account or email modal. Before consent, cloud mode made zero requests; after explicit content consent it made exactly one intercepted API request containing the extracted text and correct language pair, then reset export review state. Invalid TXT was rejected, JSON-LD parsed, and the focused consent server/browser tests passed. Browser proof at 375px dark mode and 200% text: one H1/main, no unnamed interactive control, no horizontal overflow, console error, or page error. |
| `/tools/pdf-compare/` | Canonical app | IMPROVED | Replaced the unbranded 67-character title with the focused 44-character `Compare PDFs: Text & Visual Diff | AfroTools` label and aligned visible FAQ content with its FAQ schema. Removed an unrelated generic country/amount/priority form and duplicate boilerplate FAQ, then added a main landmark, semantic settings fieldset, keyboard-operable upload targets, visible control names, pressed-state semantics, native keyboard FAQ disclosure, export-review checkpoint, responsive wrapping, and complete dark-mode contrast for upload, settings, results, text diff, visual diff, verification, and guidance surfaces. Fixed a substantive correctness defect: replacing either PDF or changing comparison settings previously left a stale comparison and downloadable report visible; both actions now clear the old result and review state. Added explicit 25 MB / 150-page responsiveness boundaries and removed the unused account/email gate. Synthetic one-page contracts produced 5 additions, 2 deletions, 7 changed words, one changed page, and 64% text similarity; visual comparison rendered one canvas and measured 0.255% pixel change. The export stayed blocked and focused its review checkbox until acknowledged, then downloaded a direct 466-byte report containing both filenames, removed and added fee values, settings, per-page notes, and the local-processing privacy note. Invalid TXT and an over-25 MB PDF were rejected; page/settings replacement tests proved stale results were hidden. There are no document-content network calls or page fetches. All four JSON-LD blocks parsed and the PDF workflow verifier passed. Browser proof at 375px dark mode and 200% text: one H1/main, no generic form, email modal, unnamed interactive control, horizontal overflow, console error, or page error. |
| `/tools/pdf-to-audio/` | Canonical app | IMPROVED | Shortened the 82-character title to the focused 51-character `PDF to Audio Reader | Local & AI Voices | AfroTools` label and aligned the five visible FAQs with FAQ schema. Removed the irrelevant country/amount form, duplicate generic FAQ/verification panel, and unused email gate. Added a main landmark, complete visible labels for voice/range/filter controls, engine pressed states, live page/status feedback, FAQ expanded semantics, a central AI document-content notice, pre-export review, 25 MB / 150-page responsiveness boundaries, small-screen reflow, and full dark contrast for upload, preview, statistics, page navigation, sidebar, FAQ, and verification surfaces. Repaired a high-risk privacy gap: AI voice generation previously sent extracted PDF text without central content consent or consent headers. AI mode now stays off after declined fresh consent, rechecks content consent before every generation, caps the current page payload at 2,800 characters, and sends both AI and document-content consent headers. A synthetic two-page handbook extracted 21 cleaned words locally, removed its URL, and made zero TTS content requests. The review gate blocked and focused before a direct 163-byte transcript containing both pages. After explicit consent, one intercepted configured-gateway request sent exactly the cleaned 85-character current-page text and selected voice; generation reset review, the audio gate blocked again, then direct export produced a 15-byte ID3 fixture. This deterministic gateway/media mock proves request, state, and export wiring; actual browser voice inventory and playback quality remain OS/browser-dependent and are not claimed from headless proof. Invalid TXT, over-25 MB, and 151-page inputs were rejected. Browser proof at 375px dark mode and 200% text: one H1/main, central consent notice present, no generic form, email modal, unnamed control, horizontal overflow, console error, or page error. |
| `/tools/pdf-bates/` | Canonical app | PENDING | |
| `/tools/html-to-pdf/` | Canonical app | PENDING | |
| `/tools/pdf-find-replace/` | Canonical app | PENDING | |
| `/tools/pdf-repair/` | Canonical app | PENDING | |
| `/tools/pdf-workflow/` | Canonical app | PENDING | |
| `/tools/cv-builder/` | Canonical app | PENDING | Confirmed baseline defect: two `h1` elements; repair in serial pass. |
| `/tools/invoice-generator/` | Canonical app | PENDING | |
| `/tools/cover-letter-generator/` | Canonical app | PENDING | |
| `/tools/meeting-minutes/` | Canonical app | PENDING | |
| `/tools/receipt-generator/` | Canonical app | PENDING | |
| `/tools/business-plan/` | Canonical app | PENDING | |
| `/tools/freelance-invoice/` | Canonical app | PENDING | |

## Evidence locations

- Hub before screenshots: `artifacts/day4-document-pdf/hub-before/`
- Hub after screenshots: `artifacts/day4-document-pdf/hub-after/`
- Per-app screenshots: `artifacts/day4-document-pdf/apps/`
- Hub workflow contract: `npm run document-pdf:verify` — PASS (31 registry tools, report sync, public planner boundary, dashboard workspace).
- PDF gate/category contract: `npm run pdf:verify` — PASS (31 registry tools, 34 HTML/app surfaces, gate coverage).
- PDF Workspace AI/privacy contracts: `node tests/ai-pdf-workspace-assist.test.js` and `node tests/ai-consent-server.test.js` — PASS.
- Source whitespace: `git diff --check` — PASS.

## Remaining risks

- Serial app audit is in progress.
- Export parser/reopen evidence must be recorded per app; a browser download event alone is not sufficient.
- Every export-capable app requires explicit signed-out direct-download proof with no modal, registration, email capture, document-content request, or parser/reopen failure.
