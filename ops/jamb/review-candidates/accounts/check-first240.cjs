const fs=require('node:fs'),path=require('node:path'),crypto=require('node:crypto'),a=require('node:assert/strict'),iv=require('./source-inventory.json'),files=[1,2,3,4,5,6].map(n=>`accounts-${String(n).padStart(3,'0')}.json`),b=files.map(f=>require('./'+f)),r=b.flatMap(x=>x.records),ids=r.map(x=>x.id);
a.deepEqual(ids,iv.eligible_ids.slice(0,240));a.equal(new Set(ids).size,240);a.equal(r.filter(x=>x.candidate).length,177);a.equal(r.filter(x=>x.status==='held').length,63);for(const x of r)a.equal(x.source_pdf_sha256,iv.source.content_sha256);
const receipt={schema_version:1,status:'first240-private-review-complete',counts:{examined:240,candidates:177,held:63,remaining_unchecked:185},source_id:iv.source_id,source_sha256:iv.source.content_sha256,next_id:iv.eligible_ids[240],batch_pins:files.map((f,i)=>({file:f,sha256:crypto.createHash('sha256').update(fs.readFileSync(path.join(__dirname,f))).digest('hex'),counts:b[i].counts})),held_ids:r.filter(x=>x.status==='held').map(x=>x.id),limitations:'Private candidates; no shared-bank integration or production proof.'},target=path.join(__dirname,'first240-coverage.json');if(process.argv.includes('--write'))fs.writeFileSync(target,JSON.stringify(receipt,null,2)+'\n');else a.deepEqual(JSON.parse(fs.readFileSync(target)),receipt);console.log(JSON.stringify({pass:true,...receipt.counts,next_id:receipt.next_id}));
