Verification-label amendment: first69 Literature candidates in001–004 now use ai-source-checked instead of ai-calculation-checked. Exact old/new fingerprints are in source-method-amendment.json, anchored to base commit47c0bd2d. Only candidate verification.method, corresponding private semantic method, and resulting content fingerprint changed. Questions, options, answers, passages, explanations, originals and holds are byte-equivalent after reversing those three fields.

First160, first320 and full361 batch pins regenerated consistently. Generator and strict checker now require source-checked for every Literature candidate. check-source-method-amendment.cjs proves69 exact method-only changes against the committed base, plus explanation-mutation fingerprint negatives. All9 answer checkers pass402 wrong-answer negatives;001–004 candidate-applied/held guards pass. Current coordinator temporary gate accepts134 candidates and rejects134 changed fingerprints. Immutable-original replay361 and all coverage receipts pass.

No shared source-pool or ledger writes. Coordinator must migrate each existing old fingerprint to its pinned new fingerprint together with the exact candidate before final release. Prior mobile content results remain applicable because all questions/passages/answers/explanations are unchanged; no production-label browser proof is claimed here.
