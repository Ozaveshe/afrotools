# Copy to /etc/openclaw/openclaw.env when using the systemd service.
# The config file at /home/openclaw/.openclaw/openclaw.json can reference these
# values with ${OPENCLAW_GATEWAY_TOKEN} and other env substitutions.

OPENCLAW_GATEWAY_TOKEN=replace-with-a-long-random-token
OPENCLAW_LOG_LEVEL=info
