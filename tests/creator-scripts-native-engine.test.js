const assert=require("node:assert/strict"),fs=require("node:fs"),path=require("node:path"),vm=require("node:vm");const context={console,Date,Math,JSON};context.window=context;vm.runInNewContext(fs.readFileSync(path.join(__dirname,"..","engines","src","creator-scripts-engine.js"),"utf8"),context);const engine=context.AfroTools.CreatorScriptsEngine;const result=engine.generateLocalScript({topic:"Entreprise créative",keyPoints:"Offre claire\nCoûts suivis\nPérimètre écrit",format:"youtube"},"fr");assert.equal(result.sections.length,4);assert.equal(result.language,"fr");assert.equal(result.title,"Entreprise créative");assert.match(result.fullScript,/1\. Offre claire/);assert.ok(result.wordCount>10);assert.match(result.estimatedDuration,/^\d+:\d{2}$/);assert.throws(()=>engine.generateLocalScript({topic:"",keyPoints:"x"},"fr"),/topic/);assert.throws(()=>engine.generateLocalScript({topic:"x",keyPoints:""},"fr"),/key point/);console.log("creator-scripts native engine: 8 assertions passed");
