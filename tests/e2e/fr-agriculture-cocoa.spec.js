const{test,expect}=require('@playwright/test'),ai=require('../../assets/js/ai/french-route-map.generated.js');
async function sentinel(page){const response=await page.request.get('/tests/fixtures/fr-agriculture-worktree-7e83-sentinel.txt');expect(response.ok()).toBe(true);const text=await response.text();expect(text).toContain('worktree=7e83');expect(text).toContain('root=C:\\Users\\Oza\\.codex\\worktrees\\7e83\\afrotools')}
function failures(page){const found=[];page.on('console',m=>{if(m.type()==='error')found.push('console:'+m.text())});page.on('pageerror',e=>found.push('pageerror:'+e.message));page.on('requestfailed',r=>{const u=new URL(r.url());if(['127.0.0.1','localhost'].includes(u.hostname))found.push('requestfailed:'+r.url())});page.on('response',r=>{if(r.status()>=400)found.push('http:'+r.status()+' '+r.url())});return found}
async function buffer(download){const stream=await download.createReadStream(),chunks=[];for await(const chunk of stream)chunks.push(chunk);return Buffer.concat(chunks)}
const input={countryCode:'GH',regionId:'gh_ar',farmSizeHa:3.5,treeAge:'old_26_35',variety:'local_forastero',management:'minimal_pruning',shade:'heavy_shade',disease:'some_control',qualityGrade:'grade_1',certification:'fairtrade'};
test('accepted English Cocoa workflow uses canonical engine',async({page})=>{await sentinel(page);const found=failures(page);await page.addInitScript(()=>localStorage.setItem('afrotools_cookie_consent','rejected'));await page.goto('/agriculture/cocoa-tracker/');await page.selectOption('#sel-country',input.countryCode);await page.selectOption('#sel-region',input.regionId);await page.fill('#inp-size',String(input.farmSizeHa));await page.selectOption('#sel-age',input.treeAge);await page.selectOption('#sel-variety',input.variety);await page.selectOption('#sel-management',input.management);await page.selectOption('#sel-shade',input.shade);await page.selectOption('#sel-disease',input.disease);await page.selectOption('#sel-grade',input.qualityGrade);await page.selectOption('#sel-cert',input.certification);await page.getByRole('button',{name:/Calculate Cocoa/}).click();const expected=await page.evaluate(v=>window.AfroTools.CocoaEngine.calculate(v),input);await expect(page.locator('#yield-stats')).toContainText(expected.modeA.totalYield.toLocaleString('en-US'));expect(found).toEqual([])});
test('French Cocoa physical-route acceptance',async({page,context})=>{await sentinel(page);const found=failures(page);await context.grantPermissions(['clipboard-read','clipboard-write']);await page.addInitScript(()=>localStorage.setItem('afrotools_cookie_consent','rejected'));await page.emulateMedia({colorScheme:'dark'});await page.setViewportSize({width:375,height:900});await page.goto('/fr/agriculture/cocoa-tracker/');await expect(page.locator('iframe')).toHaveCount(0);await expect(page.locator('html')).toHaveAttribute('lang','fr');await expect(page.locator('link[rel=canonical]')).toHaveAttribute('href','https://afrotools.com/fr/agriculture/cocoa-tracker/');await expect(page.locator('link[rel=alternate][hreflang=en]')).toHaveAttribute('href','https://afrotools.com/agriculture/cocoa-tracker/');expect((await page.locator('script[type=\"application/ld+json\"]').first().evaluate(e=>JSON.parse(e.textContent))).inLanguage).toBe('fr');expect(ai.routes['/agriculture/cocoa-tracker/']).toBe('/fr/agriculture/cocoa-tracker/');await expect(page.getByText(/aucune saisie envoyée à un serveur/i)).toBeVisible();expect(await page.locator('.card').first().evaluate(e=>getComputedStyle(e).backgroundColor)).toBe('rgb(21, 34, 55)');
await page.selectOption('#country',input.countryCode);await page.selectOption('#region',input.regionId);await page.fill('#farmSize',String(input.farmSizeHa));await page.selectOption('#treeAge',input.treeAge);await page.selectOption('#variety',input.variety);await page.selectOption('#management',input.management);await page.selectOption('#shade',input.shade);await page.selectOption('#disease',input.disease);await page.selectOption('#grade',input.qualityGrade);await page.selectOption('#certification',input.certification);const button=page.getByRole('button',{name:'Calculer la performance cacao'});await button.focus();await page.keyboard.press('Enter');await expect(page.locator('#resultPanel')).toBeVisible();const runtime=await page.evaluate(v=>({latest:window.__FR_AGRI_TEST__.latest,expected:window.__FR_AGRI_TEST__.engine.calculate(v),report:window.__FR_AGRI_TEST__.reportObject()}),input);expect(runtime.latest).toEqual(runtime.expected);expect(runtime.report.sources.donneesEnDirect).toBe(false);
for(const item of[{name:'Exporter en TXT',ext:'.txt',verify:v=>expect(v.toString('utf8')).toContain('Confidentialité')},{name:'Exporter en CSV',ext:'.csv',verify:v=>expect(v.toString('utf8')).toContain('rendement_kg_ha')},{name:'Exporter en JSON',ext:'.json',verify:v=>expect(JSON.parse(v.toString('utf8')).entrees.countryCode).toBe('GH')},{name:'Exporter en PDF',ext:'.pdf',verify:v=>expect(v.subarray(0,4).toString('ascii')).toBe('%PDF')}]){const promise=page.waitForEvent('download');await page.getByRole('button',{name:item.name}).click();const download=await promise;expect(download.suggestedFilename().endsWith(item.ext)).toBe(true);item.verify(await buffer(download))}
await page.getByRole('button',{name:'Enregistrer dans ce navigateur'}).click();expect(JSON.parse(await page.evaluate(()=>localStorage.getItem('afrotools:fr-agriculture:cocoa'))).entrees.countryCode).toBe('GH');await page.getByRole('button',{name:'Copier'}).click();await expect.poll(()=>page.evaluate(()=>navigator.clipboard.readText())).toContain('Cacao africain');await page.emulateMedia({colorScheme:'light'});expect(await page.locator('body').evaluate(e=>getComputedStyle(e).backgroundColor)).toBe('rgb(245, 248, 252)');await page.getByRole('button',{name:'Thème sombre'}).click();for(const width of[320,375,640]){await page.setViewportSize({width,height:900});expect(await page.evaluate(()=>document.documentElement.scrollWidth<=document.documentElement.clientWidth)).toBe(true)}await page.getByRole('button',{name:'Réinitialiser'}).click();await expect(page.locator('#empty')).toBeVisible();await page.fill('#farmSize','0');await page.evaluate(()=>window.__FR_AGRI_TEST__.calculate());await expect(page.getByRole('alert')).toContainText('surface positive');await expect(page.locator('#farmSize')).toBeFocused();expect(await page.evaluate(()=>{const ids=[...document.querySelectorAll('[id]')].map(e=>e.id);return ids.filter((v,i)=>ids.indexOf(v)!==i)})).toEqual([]);expect(found).toEqual([])});
