const {test,expect}=require('@playwright/test');
const fs=require('fs');
const pdfParse=require('pdf-parse');
const routes=['/tools/cnps-guide/','/sw/zana/mwongozo-wa-cnps/'];
async function prepare(page){await page.locator('[name="task"]').selectOption('remit');await page.locator('[name="actor"]').selectOption('employer');await page.locator('[name="workerBand"]').selectOption('20-plus');await page.locator('[name="asOfDate"]').fill('2026-08-09');for(const name of ['privacyConfirmed','officialSubmissionConfirmed','receiptPlanConfirmed','riskRateConfirmed'])await page.locator(`[name="${name}"]`).check();}

for(const route of routes)test(`${route} builds a private CNPS plan and reopens all advertised exports`,async({page})=>{
  const errors=[],external=[];page.on('console',message=>{if(message.type()==='error')errors.push(message.text());});page.on('pageerror',error=>errors.push(error.message));page.on('request',request=>{const url=request.url();if(/^https?:/.test(url)&&!/^http:\/\/(127\.0\.0\.1|localhost):/.test(url))external.push(url);});
  await page.goto(route);await expect(page.locator('[data-cnps-workspace]')).toHaveAttribute('data-workflow-ready','true');await page.locator('[data-cnps-workspace] button[type="submit"]').click();await expect(page.locator('[data-status]')).toContainText(route.includes('/sw/')?'Kagua':'Review');await expect(page.locator('[data-result]')).toBeHidden();await prepare(page);await page.locator('[data-action="save"]').click();await page.locator('[name="workerBand"]').selectOption('under-20');await page.locator('[data-action="load"]').click();await expect(page.locator('[name="workerBand"]')).toHaveValue('20-plus');
  const invalid=await page.locator('[data-cnps-workspace] form').evaluate(form=>Array.from(form.elements).filter(control=>control.willValidate&&!control.checkValidity()).map(control=>control.name));expect(invalid).toEqual([]);
  await page.locator('[data-cnps-workspace] button[type="submit"]').click();await expect(page.locator('[data-result]')).toBeVisible();await expect(page.locator('[data-checklist] input')).toHaveCount(4);await expect(page.locator('[data-official-link]')).toBeVisible();await expect(page.locator('[data-schedule]')).toContainText(route.includes('/sw/')?'kila mwezi':'monthly');
  await page.locator('[data-checklist] input').nth(0).check();await page.locator('[data-checklist] input').nth(3).check();await expect(page.locator('[data-progress]')).toContainText('2 / 4');
  const jsonEvent=page.waitForEvent('download');await page.locator('[data-action="json"]').click();const jsonPath=await(await jsonEvent).path();const record=JSON.parse(fs.readFileSync(jsonPath,'utf8'));expect(record.englishId).toBe('cnps-guide');expect(record.completed).toEqual([true,false,false,true]);expect(record.plan.schedule).toBe('monthly');
  const txtEvent=page.waitForEvent('download');await page.locator('[data-action="txt"]').click();const txt=fs.readFileSync(await(await txtEvent).path(),'utf8');expect(txt).toContain('2026-08-09');expect(txt).toContain(route.includes('/sw/')?'Mpango wa maandalizi':'CNPS Côte d’Ivoire preparation plan');
  const pdfEvent=page.waitForEvent('download');await page.locator('[data-action="pdf"]').click();const pdfBuffer=fs.readFileSync(await(await pdfEvent).path());expect(pdfBuffer.subarray(0,5).toString()).toBe('%PDF-');const parsed=await pdfParse(pdfBuffer);expect(parsed.text).toContain(route.includes('/sw/')?'Mpango wa maandalizi':'CNPS Cote d’Ivoire preparation plan');
  await page.locator('[data-action="reset"]').click();await expect(page.locator('[name="workerBand"]')).toHaveValue('not-applicable');await page.locator('[data-action="import"]').click();await page.locator('[name="importFile"]').setInputFiles(jsonPath);await expect(page.locator('[data-result]')).toBeVisible();await expect(page.locator('[data-checklist] input').nth(0)).toBeChecked();await expect(page.locator('[data-checklist] input').nth(1)).not.toBeChecked();
  expect(external).toEqual([]);expect(errors).toEqual([]);
});

test('country, schedule, risk-rate and RSTI uncertainty fail closed',async({page})=>{
  await page.goto('/sw/zana/mwongozo-wa-cnps/');await prepare(page);await page.locator('[name="agency"]').selectOption('other-agency');await page.locator('[data-cnps-workspace] button[type="submit"]').click();await expect(page.locator('[data-stop-reasons]')).toContainText('Côte d’Ivoire');await expect(page.locator('[data-official-link]')).toBeHidden();
  await page.locator('[name="agency"]').selectOption('ci-cnps');await page.locator('[name="workerBand"]').selectOption('unknown');await page.locator('[name="riskRateConfirmed"]').uncheck();await page.locator('[data-cnps-workspace] button[type="submit"]').click();await expect(page.locator('[data-stop-reasons]')).toContainText('idadi ya wafanyakazi');await expect(page.locator('[data-stop-reasons]')).toContainText('ajali kazini');
  await page.locator('[name="task"]').selectOption('independent');await page.locator('[name="actor"]').selectOption('unsure');await page.locator('[name="workerBand"]').selectOption('not-applicable');await page.locator('[data-cnps-workspace] button[type="submit"]').click();await expect(page.locator('[data-stop-reasons]')).toContainText('RSTI');
});

for(const width of [320,375])test(`Swahili CNPS guide reflows at ${width}px, 200% text and both themes`,async({page})=>{
  await page.setViewportSize({width,height:900});for(const scheme of ['light','dark']){await page.emulateMedia({colorScheme:scheme,reducedMotion:'reduce'});await page.goto('/sw/zana/mwongozo-wa-cnps/');await page.evaluate(()=>{document.documentElement.style.fontSize='200%';});const size=await page.evaluate(()=>({viewport:innerWidth,scroll:document.documentElement.scrollWidth}));expect(size.scroll).toBeLessThanOrEqual(size.viewport+2);await expect(page.locator('[name="task"]')).toBeVisible();const unnamed=await page.locator('[data-cnps-workspace] input:not([type=hidden]),[data-cnps-workspace] select,[data-cnps-workspace] button').evaluateAll(nodes=>nodes.filter(node=>!node.labels?.length&&!node.getAttribute('aria-label')&&!node.textContent.trim()).length);expect(unnamed).toBe(0);await page.locator('[name="task"]').focus();await page.keyboard.press('Tab');expect(await page.evaluate(()=>document.activeElement!==document.body)).toBeTruthy();}
});
