const{test,expect}=require('@playwright/test');const apps=[{id:'bond-yield',english:'/tools/bond-yield/',swahili:'/sw/zana/mavuno-ya-hatifungani/',art:[600,400],values:{'#by-currency':'KES','#by-face':'1000000','#by-coupon':'17','#by-price':'95','#by-years':'5','#by-freq':'2'},result:'#by-results',metrics:['#by-ytm','#by-current-yield','#by-annual-coupon','#by-total-return','#by-market-price'],dirty:'#by-coupon',invalid:'#by-face'},{id:'money-market',english:'/tools/money-market/',swahili:'/sw/zana/kilinganisha-mfuko-wa-soko-la-fedha/',art:[600,400],values:{'#mmf-currency':'KES','#mmf-amount':'500000','#mmf-rate':'13','#mmf-fd-rate':'10','#mmf-days':'30'},result:'#mmf-results',metrics:['#mmf-total','#mmf-daily','#mmf-weekly','#mmf-vs-fd','#mmf-liquidity','#mmf-compare tr:nth-child(1) td:nth-child(3)'],dirty:'#mmf-rate',invalid:'#mmf-amount'}];async function fill(page,values){for(const[selector,value]of Object.entries(values)){const node=page.locator(selector);if(await node.evaluate((element)=>element.tagName==='SELECT'))await node.selectOption(value);else await node.fill(value)}}async function texts(page,selectors){return Promise.all(selectors.map((selector)=>page.locator(selector).textContent()))}for(const app of apps){test(`${app.id} matches its English owner and clears stale or invalid results`,async({page})=>{await page.goto(app.english);await fill(page,app.values);await page.locator('.btn-calc').first().click();await expect(page.locator(app.result)).toBeVisible();const oracle=await texts(page,app.metrics);await page.goto(app.swahili);await fill(page,app.values);await page.locator('.btn-calc').first().click();await expect(page.locator(app.result)).toBeVisible();expect(await texts(page,app.metrics)).toEqual(oracle);if(app.id==='money-market'){await page.locator('#mmf-copy').click();const copied=await page.evaluate(()=>navigator.clipboard.readText());expect(copied).toContain(await page.locator('#mmf-total').textContent());expect(copied).toContain('30 siku');expect(copied).toContain('Hakiki factsheet')}await page.locator(app.dirty).fill('14');await expect(page.locator(app.result)).toBeHidden();await page.locator(app.invalid).fill('0');await page.locator('.btn-calc').first().click();await expect(page.locator(app.result)).toBeHidden();expect(await page.locator(app.invalid).evaluate((node)=>node.checkValidity())).toBe(false)});}function proof(){function rgb(value){return(value.match(/[\d.]+/g)||[]).slice(0,3).map(Number)}function lum(value){const c=rgb(value).map((v)=>{v/=255;return v<=.03928?v/12.92:Math.pow((v+.055)/1.055,2.4)});return.2126*c[0]+.7152*c[1]+.0722*c[2]}function ratio(a,b){const x=lum(a),y=lum(b);return(Math.max(x,y)+.05)/(Math.min(x,y)+.05)}function background(node){let current=node;while(current){const value=getComputedStyle(current).backgroundColor;if(value&&value!=='rgba(0, 0, 0, 0)'&&value!=='transparent')return value;current=current.parentElement}return'rgb(255,255,255)'}const badText=Array.from(document.querySelectorAll('body *')).filter((node)=>{const style=getComputedStyle(node),text=Array.from(node.childNodes).some((child)=>child.nodeType===Node.TEXT_NODE&&child.textContent.trim());return text&&style.display!=='none'&&style.visibility!=='hidden'&&node.getClientRects().length}).map((node)=>({tag:node.tagName,text:node.textContent.trim().slice(0,60),value:ratio(getComputedStyle(node).color,background(node))})).filter((item)=>item.value+.01<4.5);const badBoundaries=Array.from(document.querySelectorAll('input,select,button,.ai-handoff')).map((node)=>{const style=getComputedStyle(node),parentBg=background(node.parentElement);return{tag:node.tagName,id:node.id,border:style.borderColor,parentBg,value:ratio(style.borderColor,parentBg)}}).filter((item)=>item.value+.01<3);return{badText,badBoundaries}}test('two fixed-income routes pass mobile, zoom, themes, a11y, metadata, artwork, privacy and scoped AI proof',async({page,request})=>{const failed=[],errors=[];page.on('requestfailed', (item) => { if (!['www.googletagmanager.com','www.google-analytics.com','pagead2.googlesyndication.com','www.google.com','cdn.jsdelivr.net'].includes(new URL(item.url()).hostname)) failed.push(item.url()); });page.on('pageerror',(error)=>errors.push(error.message));for(const app of apps){const external=[],writes=[],badResources=[];const onRequest=(item)=>{const url=new URL(item.url());if (!['127.0.0.1', 'localhost'].includes(url.hostname) && !['www.googletagmanager.com', 'www.google-analytics.com', 'pagead2.googlesyndication.com', 'www.google.com','cdn.jsdelivr.net'].includes(url.hostname)) external.push(item.url());if (!['GET', 'HEAD'].includes(item.method()) && !['www.googletagmanager.com', 'www.google-analytics.com', 'pagead2.googlesyndication.com', 'www.google.com','cdn.jsdelivr.net'].includes(new URL(item.url()).hostname)) writes.push(`${item.method()} ${item.url()}`)};const onResponse=(response)=>{if(response.status()>=400)badResources.push(`${response.status()} ${response.url()}`)};page.on('request',onRequest);page.on('response',onResponse);await page.setViewportSize({width:320,height:900});await page.emulateMedia({reducedMotion:'reduce',colorScheme:'light'});await page.goto(app.swahili,{waitUntil:'networkidle'});await expect(page.locator('html')).toHaveAttribute('lang','sw');await expect(page.locator('link[rel="canonical"]')).toHaveAttribute('href',`https://afrotools.com${app.swahili}`);for(const lang of['en','fr','sw'])await expect(page.locator(`link[hreflang="${lang}"]`)).toHaveCount(1);await expect(page.locator('.hero-art')).toHaveJSProperty('complete',true);expect(await page.locator('.hero-art').evaluate((img)=>[img.naturalWidth,img.naturalHeight])).toEqual(app.art);await expect(page.locator('[data-shared-ai-handoff]')).toHaveAttribute('href',`/sw/ai/?tool=${app.id}`);await expect(page.locator('.source-box')).toContainText('uhakika ni wa kati');await expect(page.locator('.source-box')).toContainText('Udhamini');await expect(page.locator('.privacy-box')).toContainText('Hakuna kiasi');for(const theme of['light','dark']){await page.locator('html').evaluate((node,value)=>{node.dataset.theme=value;node.dataset.themeChoice=value},theme);const state=await page.evaluate(proof);expect(state.badText,`${app.id}:${theme} text`).toEqual([]);expect(state.badBoundaries,`${app.id}:${theme} boundaries`).toEqual([])}for(const scheme of['light','dark']){await page.emulateMedia({reducedMotion:'reduce',colorScheme:scheme});await page.locator('html').evaluate((node)=>{delete node.dataset.theme;node.dataset.themeChoice='auto'});const state=await page.evaluate(proof);expect(state.badText,`${app.id}:system-${scheme} text`).toEqual([]);expect(state.badBoundaries,`${app.id}:system-${scheme} boundaries`).toEqual([])}await page.keyboard.press('Tab');const focus=await page.evaluate(()=>{const style=getComputedStyle(document.activeElement);return{tag:document.activeElement.tagName,width:parseFloat(style.outlineWidth),style:style.outlineStyle}});expect(focus.tag).not.toBe('BODY');expect(focus.width).toBeGreaterThanOrEqual(3);expect(focus.style).not.toBe('none');const unnamed=await page.locator('main input,main select,main button,main a').evaluateAll((nodes)=>nodes.filter((node)=>{if(node.tagName==='A'||node.tagName==='BUTTON')return!node.textContent.trim()&&!node.getAttribute('aria-label');return!node.labels?.length&&!node.getAttribute('aria-label')}).map((node)=>node.outerHTML.slice(0,160)));expect(unnamed).toEqual([]);expect(await page.evaluate(()=>document.documentElement.scrollWidth-document.documentElement.clientWidth)).toBeLessThanOrEqual(1);await page.setViewportSize({width:375,height:900});await page.addStyleTag({content:'html{font-size:200%!important}'});expect(await page.evaluate(()=>document.documentElement.scrollWidth-document.documentElement.clientWidth)).toBeLessThanOrEqual(1);page.off('request',onRequest);page.off('response',onResponse);expect(external).toEqual([]);expect(writes).toEqual([]);expect(badResources).toEqual([]);const ai=await page.context().newPage();await ai.goto(`/sw/ai/?tool=${app.id}`);await expect(ai).toHaveURL(new RegExp(`/sw/ai/\\?tool=${app.id}$`));await expect(ai.locator('.ai-local-note')).toHaveAttribute('data-ai-tool-status','not-accepted');await ai.close()}expect(failed).toEqual([]);expect(errors).toEqual([]);expect((await request.get('/sw/ai/')).ok()).toBe(true)});
