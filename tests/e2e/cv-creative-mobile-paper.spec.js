const {test,expect}=require('@playwright/test'),fs=require('fs'),pdf=require('pdf-parse'),fixture=require('../fixtures/cv-long-form');
test.use({trace:'off',screenshot:'off',video:'off'});
for(const width of [320,390])test('Swahili creative PDF preserves paper padding at '+width,async({page,baseURL},info)=>{
 await page.route('**/*',r=>new URL(r.request().url()).origin===new URL(baseURL).origin?r.continue():r.fulfill({status:204}));await page.setViewportSize({width,height:844});await page.goto('/sw/zana/mjenzi-cv/');await page.waitForFunction(()=>window.CVExportPdfQuality&&window.CVApp);await page.evaluate(f=>{Object.assign(CVApp.getState().data,f);CVApp.getState().template='creative-portfolio';CVApp.getState().country='NG';CVApp.renderAll();},fixture);
 expect(await page.locator('#cvpreview .cv-prod-creative>main').evaluate(el=>getComputedStyle(el).paddingLeft)).toBe('26px');const pending=page.waitForEvent('download');await page.evaluate(()=>CVExportUpgrade.exportPdf());const file=info.outputPath('sw-creative.pdf');await(await pending).saveAs(file);const bytes=fs.readFileSync(file);expect((await pdf(new Uint8Array(bytes))).numpages).toBe(3);expect(Math.max(...[...bytes.toString('latin1').matchAll(/\/Width\s+(\d+)/g)].map(m=>+m[1]))).toBeGreaterThanOrEqual(1190);expect(await page.evaluate(()=>document.documentElement.scrollWidth)).toBe(width);expect(await page.locator('#cvpreview').textContent()).toContain('FINALVISIBLEMARKER');
});
