const test=require("node:test");const assert=require("node:assert/strict");const engine=require("../tools/national-service-gh/ghana-service-engine.js");
test("requires a user-entered official allowance",()=>{const result=engine.calculate({planMonths:12,allowanceMonthly:"",allowanceMonths:12,dueMonths:0,agencyMonths:0,otherMonths:0});assert.equal(result.valid,false);assert.match(result.error,/official record/)});
test("rejects invalid or inconsistent months",()=>{assert.equal(engine.calculate({planMonths:13,allowanceMonthly:1000}).valid,false);const result=engine.calculate({planMonths:6,allowanceMonthly:1000,allowanceMonths:6,dueMonths:7,agencyMonths:0,otherMonths:0});assert.equal(result.valid,false);assert.match(result.error,/Due months/)});
test("calculates full plan and user-defined reconciliation",()=>{const result=engine.calculate({planMonths:10,allowanceMonthly:715,allowanceMonths:10,dueMonths:6,receivedToDate:3575,agencyMonthly:200,agencyMonths:5,otherMonthly:100,otherMonths:4,oneTimeSupport:500,housingMonthly:300,foodMonthly:400,transportMonthly:150,dataMonthly:80,otherCostMonthly:70,oneTimeCosts:600});assert.equal(result.valid,true);assert.equal(result.allowanceTotal,7150);assert.equal(result.totalIncome,9050);assert.equal(result.monthlyEssentials,1000);assert.equal(result.totalCosts,10600);assert.equal(result.remainder,-1550);assert.equal(result.dueEstimate,4290);assert.equal(result.reconciliationDifference,715)});
test("does not turn a negative reconciliation into a payment claim",()=>{const result=engine.calculate({planMonths:1,allowanceMonthly:1000,allowanceMonths:1,dueMonths:1,receivedToDate:1200,agencyMonths:0,otherMonths:0});assert.equal(result.valid,true);assert.equal(result.reconciliationDifference,-200)});
