'use strict';const test=require('node:test'),assert=require('node:assert/strict'),fs=require('node:fs'),path=require('node:path');const root=path.resolve(__dirname,'..'),read=p=>fs.readFileSync(path.join(root,p),'utf8');
test('native app replaces prior hub-only mismatch with reciprocal metadata',()=>{const s=read('sw/zana/bei-za-crypto/index.html');assert.match(s,/<html\b[^>]*\blang=["']sw["'][^>]*>/i);assert.match(s,/data-crypto-prices/);assert.match(s,/crypto-prices-vip\.js/);assert.match(s,/inLanguage":"sw"/);assert.match(s,/crypto-prices\.webp/);for(const p of['crypto/prices/index.html','fr/crypto/prices/index.html'])assert.match(read(p),/hreflang="sw" href="https:\/\/afrotools\.com\/sw\/zana\/bei-za-crypto\/"/);});
test('runtime is native, fail closed, and request/export bounded',()=>{const s=read('assets/js/pages/crypto-prices-vip.js');assert.match(s,/sw:\s*\{/);assert.match(s,/sw-KE/);assert.match(s,/status !== 'fresh'/);assert.match(s,/currency=[\s\S]*?limit=100/);assert.match(s,/freshnessCeilingMinutes/);assert.doesNotMatch(s,/localStorage|sessionStorage|api\.coingecko|innerHTML/);});
