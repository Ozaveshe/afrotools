"use strict";const test=require("node:test"),assert=require("node:assert/strict"),fs=require("node:fs"),engine=require("../assets/js/engines/transport-cost-engine.js"),{SW_TRANSPORT_COST_APPS}=require("../scripts/lib/sw-transport-cost-contract.js");
test("bounded owner accepts only deterministic fleet fuel",()=>{assert.deepEqual(SW_TRANSPORT_COST_APPS.map(x=>x.id),["fleet-fuel"]);});
test("fleet oracle preserves English 6-day week and operating-day month",()=>{const o=engine.fleetFuel({count:8,dailyKm:120,consumption:12,price:1.25,opDays:26});assert.equal(o.litresPerVehicleDay,14.4);assert.equal(o.fleetDay,144);assert.equal(o.fleetWeek,864);assert.equal(o.fleetMonth,3744);assert.equal(o.fleetAnnual,44928);assert.equal(o.saving10pct,4492.8);});
test("invalid fleet inputs fail closed",()=>{assert.equal(engine.fleetFuel({count:0,dailyKm:1,consumption:1,price:1,opDays:1}).error,"invalid-input");});
test("English and Swahili routes consume the shared DOM-free engine",()=>{for(const file of ["tools/fleet-fuel/index.html","sw/zana/gharama-mafuta-ya-fleet/index.html"])assert.match(fs.readFileSync(file,"utf8"),/transport-cost-engine\.js/);const runtime=fs.readFileSync("assets/js/pages/sw-transport-cost-parity.js","utf8");assert.match(runtime,/AfroLocalOnly=true/);assert.doesNotMatch(runtime,/fetch\(|XMLHttpRequest|localStorage/);});
