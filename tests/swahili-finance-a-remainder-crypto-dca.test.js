'use strict';const assert=require('node:assert/strict'),fs=require('node:fs'),path=require('node:path'),engine=require('../assets/js/engines/crypto-dca-replay.js'),root=path.resolve(__dirname,'..'),read=f=>fs.readFileSync(path.join(root,f),'utf8');
const sw=read('sw/zana/urejeshaji-wa-dca-ya-crypto/index.html'),js=read('assets/js/pages/crypto-dca-replay.js'),en=read('crypto/dca-calculator/index.html'),fr=read('fr/crypto/dca-calculator/index.html');
assert.match(sw,/<html lang="sw"/);assert.match(sw,/"inLanguage":"sw"/);assert.match(sw,/CoinGecko/);assert.match(sw,/si utabiri wa bei wala pendekezo/);assert.match(sw,/crypto-dca\.webp/);assert.match(js,/sw: \{/);assert.match(en,/hreflang="sw"/);assert.match(fr,/hreflang="sw"/);assert.doesNotMatch(js,/fetch\([^)]*contribution|sendBeacon|localStorage/i);
const prices=[0,1,2].map(i=>({at:`2026-08-0${6+i}T00:00:00.000Z`,price:100+i*10}));const out=engine.replay({contribution:1000,percentCost:1,fixedCost:10,startDate:'2026-08-06',endDate:'2026-08-08',frequency:'daily'},prices);assert.equal(out.usedCount,3);assert.equal(out.totalOutlay,3000);assert.equal(out.totalCosts,60);assert.ok(out.totalUnits>0);assert.equal(engine.buildSchedule('2026-08-06','2026-08-08','daily').length,3);console.log('sw crypto DCA static: 14 assertions passed');
