const {test}=require('node:test'),assert=require('node:assert/strict'),path=require('node:path'),quality=require('../scripts/lib/calculation-quality'),registration=require('../scripts/lib/calculation-quality-senegal-leave');
test('Senegal engine has explicit high legal risk and versioned review-required provenance',()=>{const c=quality.classifyArtifact('assets/js/engines/senegal-child-leave.js');assert.equal(c.riskDomain,'legal_regulatory');assert.equal(c.riskLevel,'high');const m=registration.metadata('assets/js/engines/senegal-child-leave.js');assert.deepEqual(m.jurisdictions,['SN']);assert.equal(m.currency,null);assert.equal(m.supportStatus,'review-required');assert.equal(m.effectiveDateStatus,'review-required');assert.match(m.sources[0].title,/Commencement unverified/);assert.deepEqual(registration.metadata('unrelated'),{});});
test('twelve independent Senegal fixtures execute and detect incorrect child addition',()=>{const root=path.resolve(__dirname,'..'),data=quality.loadQualityArtifacts(root);data.fixtures.fixtures=data.fixtures.fixtures.filter(f=>f.operation==='senegal-child-leave');assert.equal(data.fixtures.fixtures.length,12);let result=quality.runGoldenFixtures(data,root);assert.equal(result.passed,12);assert.deepEqual(result.failures,[]);data.fixtures.fixtures.find(f=>f.id==='senegal-leave-two-children').expected.totalDays=28;result=quality.runGoldenFixtures(data,root);assert.equal(result.failures.length,1);});
