'use strict';const test=require('node:test'),assert=require('node:assert/strict'),fs=require('node:fs'),path=require('node:path'),root=path.resolve(__dirname,'..'),read=p=>fs.readFileSync(path.join(root,p),'utf8');
test('native Swahili owner and reciprocal route are exact',()=>{const s=read('sw/zana/ulinganisho-nukuu-za-kutuma-fedha/index.html');assert.match(s,/<html[^>]*lang="sw"/);assert.match(s,/data-locale="sw"/);assert.match(s,/crypto-remittance\.webp/);assert.match(s,/"inLanguage": "sw"|"inLanguage":"sw"/);for(const p of['crypto/remittance/index.html','fr/crypto/remittance/index.html'])assert.match(read(p),/hreflang="sw" href="https:\/\/afrotools\.com\/sw\/zana\/ulinganisho-nukuu-za-kutuma-fedha\/"/);});
test('runtime is native and local-first',()=>{const s=read('assets/js/pages/remittance-quote-comparator.js');assert.match(s,/root\.dataset\.locale==="sw"/);assert.match(s,/Ulinganisho umesasishwa ndani ya kifaa/);assert.doesNotMatch(s,/fetch\(|XMLHttpRequest|localStorage|sessionStorage/);});
test('generator owns output and visible app copy has no English form residue',()=>{const s=read('sw/zana/ulinganisho-nukuu-za-kutuma-fedha/index.html');for(const x of['Enter the quote','Required receipt','Recipient amount','Total debit','Compare entered'])assert.doesNotMatch(s,new RegExp(x));assert.match(read('scripts/build-sw-crypto-remittance.js'),/crypto\/remittance\/index\.html/);});
