const test=require('node:test');const assert=require('node:assert/strict');const e=require('../../assets/js/engines/lr-vat.js');const api=require('../../netlify/functions/api-vat.js');test('Liberia standard add and extract use enacted 13%',()=>{assert.equal(e.calculate({amount:1000}).gst,130);assert.equal(e.calculate({amount:1130,mode:'extract'}).net,1000)});test('zero-rated export goods fails closed',()=>{assert.throws(()=>e.calculate({amount:100,rateKind:'confirmed-export-goods-zero'}),x=>x.code==='RATE_EVIDENCE_REQUIRED');assert.equal(e.calculate({amount:100,rateKind:'confirmed-export-goods-zero',rateEvidenceConfirmed:true,rateEvidenceType:e.ZERO_EVIDENCE}).gst,0)});test('API accepts only exact Section 1000 export evidence',()=>{const v=api._test.validateLiberiaRateRequest;assert.deepEqual(v({}),{ok:true,rate:13});assert.equal(v({customRate:0}).ok,false);assert.equal(v({customRate:0,rateEvidenceConfirmed:true,rateEvidenceType:e.ZERO_EVIDENCE}).ok,true);assert.equal(v({customRate:15,rateEvidenceConfirmed:true,rateEvidenceType:'telecom'}).ok,false)});
