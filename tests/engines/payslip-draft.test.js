'use strict';const test=require('node:test');const assert=require('node:assert/strict');const engine=require('../../assets/js/engines/payslip-draft.js');
const fixture={confirmed:true,basic:100000,housing:20000,transport:10000,overtime:5000,bonus:3000,otherEarnings:2000,reimbursement:4000,paye:12000,pension:8000,social:3000,loan:2000,otherDeductions:1000,employerContribution:6000,currency:'NGN'};
test('earnings, reimbursements, deductions and employer cost reconcile',()=>{const r=engine.calculate(fixture);assert.equal(r.gross,140000);assert.equal(r.totalPaid,144000);assert.equal(r.totalDeductions,26000);assert.equal(r.netPay,118000);assert.equal(r.employerCost,150000);});
test('zero values remain explicit and do not create tax assumptions',()=>{const r=engine.calculate({...fixture,paye:0,pension:0,social:0,loan:0,otherDeductions:0});assert.equal(r.netPay,144000);assert.equal(r.deductions.length,5);});
test('confirmation, positive earnings and deduction bounds are enforced',()=>{assert.throws(()=>engine.calculate({...fixture,confirmed:false}),/Confirm/);assert.throws(()=>engine.calculate({...fixture,basic:0,housing:0,transport:0,overtime:0,bonus:0,otherEarnings:0}),/earnings/);assert.throws(()=>engine.calculate({...fixture,paye:200000}),/cannot exceed/);});
