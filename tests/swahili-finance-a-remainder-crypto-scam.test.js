'use strict';const test=require('node:test'),assert=require('node:assert/strict'),fs=require('node:fs'),path=require('node:path'),root=path.resolve(__dirname,'..'),read=p=>fs.readFileSync(path.join(root,p),'utf8');test('native sensitive-data owner and reciprocal metadata are exact',()=>{const s=read('sw/zana/kifurushi-ushahidi-ulaghai-crypto/index.html');assert.match(s,/<html lang="sw"/);assert.match(s,/crypto-scam\.webp/);assert.match(s,/"inLanguage": \[\s*"sw"/);for(const p of['crypto/scam-checker/index.html','fr/crypto/scam-checker/index.html'])assert.match(read(p),/hreflang="sw" href="https:\/\/afrotools\.com\/sw\/zana\/kifurushi-ushahidi-ulaghai-crypto\/"/);});test('dynamic copy is native and no storage or network is introduced',()=>{const s=read('assets/js/pages/crypto-scam-evidence.js');assert.match(s,/documentElement\.lang === "sw"/);assert.match(s,/Kifurushi kimepangwa ndani ya kifaa/);assert.doesNotMatch(s,/fetch\(|XMLHttpRequest|localStorage|sessionStorage/);});test('visible form has no material English residue',()=>{const s=read('sw/zana/kifurushi-ushahidi-ulaghai-crypto/index.html');for(const x of['Build your private','Incident label','Evidence items','Record status','Never enter a private','Does this tool'])assert.doesNotMatch(s,new RegExp(x));});
