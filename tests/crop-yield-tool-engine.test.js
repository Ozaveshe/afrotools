'use strict';const assert=require('node:assert/strict'),data=require('../data/agriculture/crop-yield-tool-data'),engine=require('../engines/src/crop-yield-tool-engine');let count=0;for(const crop of Object.keys(data.crops))for(const sizeUnit of['ha','acres','plots'])for(const currency of Object.keys(data.symbols))for(const farmSize of[.5,5,50]){const r=engine.calculate({crop,sizeUnit,currency,farmSize,yieldPerHa:data.crops[crop].defaultYield||2.75,marketPrice:150000,inputCosts:200000},data);assert.equal(r.ok,true);assert.equal(r.totalYield,r.sizeHa*r.input.yieldPerHa);count+=1}assert.equal(engine.calculate({farmSize:0},data).status,'missing-size');assert.equal(engine.calculate({farmSize:1,yieldPerHa:0},data).status,'missing-yield');assert.equal(engine.calculate({farmSize:1,yieldPerHa:1,marketPrice:0},data).status,'missing-price');console.log(`PASS ${count} Crop Yield Tool engine scenarios`);
