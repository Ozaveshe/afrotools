'use strict';const test=require('node:test'),assert=require('node:assert/strict'),fs=require('node:fs'),path=require('node:path'),root=path.resolve(__dirname,'..'),read=p=>fs.readFileSync(path.join(root,p),'utf8');test('native owner and reciprocal metadata are exact',()=>{const s=read('sw/zana/marejeo-ya-stablecoin/index.html');assert.match(s,/<html lang="sw"/);assert.match(s,/crypto-stablecoins\.webp/);for(const p of['crypto/stablecoins/index.html','fr/crypto/stablecoins/index.html'])assert.match(read(p),/hreflang="sw" href="https:\/\/afrotools\.com\/sw\/zana\/marejeo-ya-stablecoin\/"/);});test('runtime is native and fresh-only',()=>{const s=read('assets/js/pages/crypto-stablecoins-vip.js');assert.match(s,/documentElement\.lang === 'sw'/);assert.match(s,/sw-KE/);assert.match(s,/payload\.status !== 'fresh'/);assert.match(s,/provider_reference_not_exchange_quote/);assert.doesNotMatch(s,/localStorage|sessionStorage|api\.coingecko/);});
