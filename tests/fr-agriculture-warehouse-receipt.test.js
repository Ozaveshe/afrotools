'use strict';
const assert=require('node:assert/strict'),fs=require('node:fs'),path=require('node:path'),engine=require('../engines/src/warehouse-receipt-engine'),data=require('../data/agriculture/warehouse-receipt-data'),manifest=require('../data/localization/fr-agriculture-parity-manifest.json'),ai=require('../assets/js/ai/french-route-map.generated.js');
const{ROOT,assertNativeFrenchOutput}=require('../scripts/lib/fr-agriculture-parity-manifest'),row=manifest.rows.find(value=>value.english.id==='warehouse-receipt');assert.ok(row);assertNativeFrenchOutput(manifest,[row.french.route]);const html=fs.readFileSync(path.join(ROOT,row.french.file),'utf8');assert.doesNotMatch(html,/<iframe\b|\bfetch\s*\(/i);assert.match(html,/warehouse-receipt-engine\.js/);assert.match(html,/aucune saisie envoyée à un serveur/i);assert.match(html,/aucun taux, prix, agrément ou opérateur vérifié en direct/i);assert.equal(ai.routes[row.english.routeKey],row.french.routeKey);const profiles=[];for(const[countryCode,country]of Object.entries(data.countries))for(const commodity of Object.keys(data.commodities))for(const periodMonths of[1,4,12]){const input={countryCode,commodity,quantityTonnes:20,harvestPricePerTonne:85000,ltvPct:country.ltv,annualRatePct:country.rate,periodMonths,storagePerTonneMonth:country.storage,insuranceAnnualPct:country.insurance,handlingPerTonne:country.handling,priceIncreasePct:data.commodities[commodity].increase},output=engine.calculate(input,data);assert.equal(output.ok,true);profiles.push({input,output})}const oracle={schemaVersion:1,family:'singleton:warehouse-receipt',profiles};if(process.env.FR_AGRI_ORACLE_OUTPUT)fs.writeFileSync(path.resolve(ROOT,process.env.FR_AGRI_ORACLE_OUTPUT),JSON.stringify(oracle,null,2)+'\n');console.log(JSON.stringify({family:oracle.family,rows:1,profiles:profiles.length},null,2));
