const{test}=require('node:test');const assert=require('node:assert/strict');const fs=require('fs');const source=fs.readFileSync('assets/js/pages/cover-letter-generator.js','utf8');const start=source.indexOf('    function scoreLetter('),end=source.indexOf('    function nativeOption(',start);const score=new Function('d',source.slice(start,end)+';return scoreLetter;')(Object.create(null));const data={fullName:'Synthetic',email:'synthetic@example.test',phone:'',jobTitle:'Analyst',company:'Example',skills:'analysis, communication',achievement:'20',whyCompany:'A sufficiently detailed reason for applying to this employer.',jobDescription:'',resumeSummary:''};
for(const[locale,text]of [['en','Dear Hiring Manager,\nEvidence.\nYours sincerely,'],['fr','Madame, Monsieur,\nExemple.\nCordialement,'],['sw','Ndugu waajiri,\nMfano.\nWenu mwaminifu,']])test('native structure is recognized: '+locale,()=>{assert.equal(score(data,text,locale).checks.find(x=>x.label==='Professional structure').good,true);assert.equal(score(data,'Plain text without greeting or closing',locale).checks.find(x=>x.label==='Professional structure').good,false);});
test('French accents and whole words stay intact in keyword matching',()=>{const result=score({...data,skills:'',jobDescription:'Avec réconciliation données data',resumeSummary:''},'réconciliation données database','fr');assert.deepEqual(result.keywords.sort(),['data','données','réconciliation']);assert.deepEqual(result.matched.sort(),['données','réconciliation']);assert.deepEqual(result.missing,['data']);assert.equal(result.words,3);});
test('French and Swahili result cues receive the same heuristic check',()=>{for(const[locale,achievement]of [['fr','J’ai réduit les délais'],['sw','Tumepunguza ucheleweshaji']])assert.equal(score({...data,achievement},'',locale).checks.find(x=>x.label==='Proof with result').good,true);});
