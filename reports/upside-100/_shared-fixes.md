# Shared-file fixes (applied centrally, not by per-tool agents)

Agents append here when a fix needs a file shared across tools (assets/**, data/** shared, engines/**, netlify/**, generators). Format: `- [tool] file → change → why`.

- [route-fares] `/api/transport-fares` endpoint + `assets/js/market-data-app.js` → the fare feed returns crowd-sourced rows with per-row `observed_at`/`currency_code`/`source_type`, but the API/app expose no aggregate freshness signal (e.g. "N fares, most recent observed <date>") and no per-card observation-date or source-type badge in the default `renderCard`. Surface an "as-of" line in the summary and add observed-date + source-type to each card so no-JS/AI readers and commuters can judge staleness → fares are volatile and currently read as undated. Tool page now carries a visible indicative-estimate + AfroPoints-source disclaimer, but the actual per-row provenance still isn't rendered.

- [tbill-calc] no shared-file change needed → all fixes (rate-type selector, discount-vs-yield math, SEO title/meta/H1, disclaimer, a11y) stayed within `tools/tbill-calc/index.html`.

- [crypto-scam] netlify/functions/crypto-scam.js (search branch) → BACKEND FOLLOW-UP: add external scam-DB cross-reference (Chainabuse / CryptoScamDB public APIs) plus address-format validation (EVM/BTC/TRON) so a database miss returns a real signal instead of silence → the lookup currently sees only AfroTools' own verified rows, producing very high false-negative rates on real scam addresses. Fail closed on missing keys; smoke-test per netlify-functions rule.


## data/exam-systems.js — classification bands (from waec-calculator, 2026-07-14)

Deferred from tool-local WAEC work (shared file, not edited). To make the classification badge meaningful for all systems, add `aggregateRanges`/classification bands to:
- `za-nsc` (APS 6–42, higher-is-better; has only `passTypes`)
- `ug-uce` (best 8, D1–F9, lower-is-better)
- `zw-zimsec` (best 6, A*–U, lower-is-better)
Bac systems (`fr-bac`, `na-bac`) already have `classifications`. Note `za-nsc` grade keys are integers, so any code deriving direction from key order must not rely on insertion order (tool uses an explicit higher-is-better set instead).


## Farm payroll — statutory rate verification (from farm-payroll-calculator, 2026-07-14)

Deferred (shared/generated data, not edited by the tool agent):
- [farm-payroll] `agriculture/farm-payroll/index.html` SEO copy + df engine / country-index statutory rates → VERIFY the Kenya agricultural minimum wage figure ("KSh 7,997/month in 2024"). The 2022 Regulation of Wages (Agricultural Industry) Order plus the 2024 Labour-Day 6% raise likely puts the unskilled agri rate slightly higher (~KSh 8,100). Confirm against the current gazette before trusting it in country-page calculations → point-in-time wage claim risks being stale for Jul 2026 payroll runs.
- Product backlog (df engine / country pages): no overtime input on the hub quick-estimator, no payslip export, and no visible "rates as-of" date next to statutory figures.


### cars directory

All changes are to GENERATED sources. Regenerate with `node scripts/generate-car-price-pages.js`, then `node scripts/sync-car-market-countries.js` (only if country list changes) and validate with `npm run test:car-price-intelligence` (+ `npm run check-links` for broad route regen). Do NOT hand-edit `cars/**`.

- **[cars — P1, biggest] `scripts/generate-car-price-pages.js`** → the root `writePage("cars", {...})` call (line ~262) passes no `pageType`, so `staticContentHTML()` returns `""` and the hub ships with only a `<noscript>` block + empty `#carsApp`. Add a hub branch to `staticContentHTML()` (e.g. `meta.pageType === "hub"`) that server-renders: (a) a 2–3 paragraph intro on what the directory does and how landed-cost vs local-ask works; (b) a "Top imports tracked across African markets" table via the existing `vehicleTableHTML(data.vehicles, defaultCountry, "/cars/<country>", …)`; (c) a country-links list (`Object.values(data.countries).filter(directory_enabled).map(c => <a href="/cars/<slug>/">)`); then pass `pageType:"hub"` (and a default `countryObj`, e.g. Nigeria) in the root `writePage`. WHY: the hub is the highest-authority URL and currently has near-zero indexable body text — this is the audit's confirmed "thin visible copy."

- **[cars — P1] `scripts/generate-car-price-pages.js`** → add a server-rendered, visible FAQ section (render `data.faqs` as `<section><h2>FAQ</h2><details><summary>Q</summary><p>A</p></details>…`) inside `staticContentHTML()` for the hub and country pages (gate on `meta.includeFaq`). WHY: the 3 FAQs exist only in JSON-LD + the JS app; no-JS crawlers and AI answer engines get schema but no readable passage. Visible FAQ text improves passage-level citability (GEO).

- **[cars — P2] `scripts/generate-car-price-pages.js`** → reconcile the `html()` template (lines ~199-245) with the deployed `cars/index.html`, which additionally carries `afro-related-tools` SSR, `assets/js/lib/source-confidence.js`, the `afro-source-meta` ledger div, `data-chat-bundle`, and analytics via bundles. A plain regen currently STRIPS these blocks. Fold the related-tools SSR + source-confidence includes into the template (or document the required post-processing step) so regeneration is safe. WHY: prevents a regen from silently removing internal links + source-trust UI already live in production.

- **[cars — P2] `scripts/generate-car-price-pages.js`** → in `schema()`, add `url` (canonical vehicle route) to each `ItemList` `ListItem`, and on `pageType:"vehicle"` add a `Vehicle`/`Product` JSON-LD block with `offers`/`AggregateOffer` `lowPrice`/`highPrice`/`priceCurrency` from `vehicle.price`. WHY: ItemList items currently carry only `name` (no crawl/citation value); Product price-range markup is eligible for rich results and none of the marketplace competitors expose landed-cost price ranges.

- **[cars — P3] `data/cars/price-intelligence.json`** → expand `vehicles` beyond the current 25 Toyota-heavy rows (add Nissan, Honda, Mazda, Mitsubishi, Suzuki, VW, Mercedes, and common SUV/pickup demand) and add per-vehicle `depreciationCurve`/TCO fields the templates can render. WHY: catalogue depth undercuts the 20-market claim; depreciation/TCO buyer-guidance is the defensible content gap vs Cars45/AutoChek/Jiji/Carmudi (who are inventory marketplaces, not decision tools).

- **[cars — P3] `data/cars/price-intelligence.json`** → enrich `countryContent` with a per-country age-eligibility rule explainer + a short "import vs buy-local" verdict paragraph, and surface it in the country-page `countryEditorialHTML()`. WHY: adds unique, non-templated depth per country page (thin-content and E-E-A-T uplift) and directly serves the top buyer question competitors don't answer.

- **[cars — data-trust note] `scripts/generate-car-price-pages.js`** → the static price tables bake FX from `data/forex/latest.json` at generate time; make the static note state the FX snapshot date explicitly (from `forex.timestamp`) rather than the vague "update live inside the directory," so a no-JS reader knows the band's as-of date. WHY: avoids a stale baked band reading as current. (Directory-estimate labelling for the 14 non-rule-pack markets is already correct — keep it.)

## scholarship-finder — deferred features/shared-file work (2026-07-14)

- [scholarship-finder] catalog schema (`_shared/scholarship-platform.js`) + both profile cards + `applyFilters` in `tools/scholarship-finder/index.html` → FEATURE: add nationality / country-of-origin eligibility filter (an `eligible_countries` catalog field + a select in Quick Check and Full Explorer) → biggest competitor gap; tool scores destination but never asks the student's African country, so it can surface nationality-restricted awards (Commonwealth list, DAAD by country, Mastercard Foundation) the student is ineligible for.
- [scholarship-finder] shared build/SSR pipeline → DEFERRED: server-render the ~10 curated fallback cards as static `.sch-card`s (progressive enhancement overwrites them live) → grid is 100% client-rendered today, so crawlers see only intro + FAQ; SSR gives indexable content, no-JS support, and lets the new ItemList schema reflect visible DOM. Needs a shared generator, not a hand edit.
- [scholarship-finder] `assets/js/education-scholarship-feed.js` (shared) vs inline `FALLBACK` in `tools/scholarship-finder/index.html` (~558–569) → DEFERRED: single-source the 10-item FALLBACK array (currently declared in both, can drift on min_gpa/deadline_text) → shared file; verify no `scholarship-*` test snapshot depends on the inline copy before consolidating.

## education-hub — related-tools SSR monogram icons
- `assets/js/components/related-tools.min.js` emits tool cards with 2-letter monograms (`data-icon="WN"`, "AS", "SA"...) instead of inline Lucide-style SVG line-icons. Site convention (per memory) is SVG line-icons. Fix in the shared component so every consumer page benefits. Seen on /tools/education-hub/.

## tenancy-agreement — country-coverage depth + export (2026-07-14)
- [tenancy-agreement] per-country pages under `tools/tenancy-agreement/<country>.html` (54 generated) → VERIFY: the interactive generator only has tailored legal text (`LAWS` object) for 8 countries (NG, KE, GH, ZA, TZ, UG, RW, ET). Confirm the other 46 dedicated country pages carry real, jurisdiction-specific clause/notice/deposit data and are not generic NG boilerplate. Whatever generator builds these should source per-country notice periods + deposit norms. Tool-local hero/meta reworded to stop the "54 countries" overclaim on the hub page.
- [tenancy-agreement] shared export pipeline → DEFERRED FEATURE: real PDF + DOCX export and e-signature to match LawDepot/Rocket Lawyer. Current export is `window.print()` + plain-text download only. Needs shared lib, not a hand edit.
- [tenancy-agreement] page uses inline `--blue:#1d4ed8` accents instead of brand `var(--color-primary)` (#0062CC). Non-brand accent; left this pass (surgical scope). Migrate when the legal-category recolour pass runs.

## farm-profit-calculator — shared engine break-even refinement (2026-07-14)
- [farm-profit-calculator] `engines/farm-profit-engine.js` → DEFERRED (shared file, not edited): `breakEvenYield` / `breakEvenYieldPerHa` / `breakEvenPrice` are computed from gross price and total yield (`W/d`, `W/p`) and ignore the post-harvest-loss haircut and the revenue-scaled marketing/middleman fees. So with a 25% PH loss the displayed break-even yield (e.g. 5.47 t/ha) understates the net-of-loss yield actually needed to cover costs (~7.29 t/ha). Core P&L is correct — Net Profit = Net Revenue − Total Cost verified via node across profit/loss/family-labor/process cases; ROI, margin, cost-of-production all reconcile. Fix: divide break-even by `(1 - postHarvestLossPct/100)` (and optionally by `(1 - (marketFeesPct+middlemanPct)/100)`) so the metric reflects deliverable-to-market yield. Affects all 54 country pages under `agriculture/farm-profit/`.

## brideprice-advisor — FR mirror sync (2026-07-14)
- [brideprice-advisor] `/fr/tools/conseiller-dot/` → FR PIPELINE (not hand-edited per docs/french-localization-repair.md). The EN tool at `tools/brideprice-advisor/index.html` had tone + SEO fixes this pass that the FR mirror must inherit: (1) meta/OG/Twitter descriptions rewritten ("13 cultures / 8 countries", dignified framing, dropped "fun comparisons" lead); (2) the "😂 Fun Comparisons" section replaced by "🕊️ Putting It in Context" (income-share + savings-horizon framing; the `funFacts` DATA arrays with iPhone/jollof/"Budget King"/"cheapest in Africa" text were deleted and the render now computes from `INCOME_AVG`); (3) a persistent `.bp-notice` disclaimer banner added above the tool ("cultural-education estimate, not a literal price or demand"); (4) hero badge "Fun Comparisons" → "Cultural Context"; (5) added FAQPage JSON-LD + `applicationCategory` → `LifestyleApplication`. Re-run the FR localization pipeline so the accented FR copy mirrors these strings — do not hand-author unaccented FR text.

## Inline CPI presets — freshness (from inflation-calc, 2026-07-14)

`tools/inflation-calc/index.html` hardcodes single-point 2024 annual inflation rates in the inline `COUNTRIES` array (e.g. Nigeria 33.4%, Ghana 23.2%, Egypt 28.3%). As of mid-2026 these are ~2 years stale and several have moved materially. There is no shared data file or source ledger for African CPI. Recommend: extract to a dated `data/**` file (or default the tool to the World Bank `FP.CPI.TOTL.ZG` fetch it already implements) so the default first paint shows current, sourced data. Not editable tool-locally without a shared surface.

## itax-guide — high-intent money tool lacks in-body business CTA (2026-07-14)
- [itax-guide] `tools/itax-guide/index.html` is a high-intent KRA-filing guide (login/PIN/P9/NIL/deadline) that today monetises only via the shared related-tools SSR strip (AfroTools Pro card) and free-calculator links. There is no in-body conversion CTA — e.g. a "get filing help" / AfroTools Pro upsell / lead capture near the P9 filing section or the readiness checklist. Adding one is a real revenue upside but touches conversion/monetisation strategy and possibly a shared CTA component, so it was left out of this surgical pass. Decide the offer (Pro upsell vs. filing-assist lead) before implementing.

- [binary-converter] No shared-file edits were required. All fixes stayed within `tools/binary-converter/index.html`. Noted only: green accent residue (`rgba(0,135,81,…)` / `rgba(93,219,158,…)`) existed in the tool's inline CSS despite the blue brand — fixed locally to `rgba(0,98,204,…)`/`rgba(77,163,255,…)`. If this green-residue pattern recurs in other single-file tools migrated from the old green theme, worth a sweep.

- [diaspora-guide] No shared-file changes required. All fixes confined to tools/diaspora-guide/index.html (title, meta, FAQPage JSON-LD to match visible FAQ, static disclaimer, internal tax-tool links).

## fuel-tracker — freshness + source attribution (2026-07-14)
- [fuel-tracker] Displayed as-of date reads "Updated 12 Jun 2026" (`#fuelDeskState` + repeated at index.html ~L95-96, L112, L678 and in `/data/fuel/latest.json`) → STALE (~1 month) as of 14 Jul 2026, matching the known scraper/automation outage (memory: down since ~Jun 30). Snapshot is JS/data-pipeline driven — NOT hand-edited per fuel data rules. Restore the fuel scraper / refresh `latest.json` so the visible snapshot and Dataset `dateModified`/`temporalCoverage` advance.
- [fuel-tracker] Price source attribution is a generic "saved fuel price snapshot" label from the shared `afro-source-meta` component (`data-source-meta-id="afrofuel-static-snapshot"`). Competitors (GlobalPetrolPrices) cite a named methodology/source. Consider per-market named authority sources (NNPC/NMDPRA, Ghana NPA, Kenya EPRA, SA DMRE, etc.) in the shared source layer so AfroFuel can show a real source name + link, not just a date. Shared-component change, not tool-local.

## CRITICAL bugs found by Wave 5-7 agents that hit the session rate limit before fixing (2026-07-14)
These were DIAGNOSED but NOT yet fixed (agents terminated by "session limit"). Fix on resume:

- [smart-contract-scanner] crypto/contract-scanner/index.html → **FIXED by main loop 2026-07-14**: checkScamDB read `scamDB.addresses` (undefined; data file key is `reports`) so known scams were NEVER flagged and every valid address showed a GREEN "Low Risk" all-clear. Fixed lookup to read `.reports`; replaced the green "Low Risk" downgrade with amber "Not Flagged — Unverified" (verified: known DB addr → High Risk, random addr → Unverified).
- [nda-generator] tools/nda-generator/index.html (~L420-422) → stray `</script>` inside a `document.write` string breaks the print/export window. (Static H1=1 OK; 16-country claim matches.) NOT fixed.
- [qr-generator] tools/qr-generator/index.html → meta/OG/Twitter/JSON-LD claim "logo overlay" support but there is NO logo feature (overclaim to remove); SVG download queries `#qrcode svg` but qrcodejs 1.0.0 renders a `<canvas>` → SVG download is broken. NOT fixed.
- [greenhouse-cost-estimator] agriculture/greenhouse/index.html (JS ~L341-396) → inline JS appends a 15-country set to `#countryHub` which ALREADY has static cards → country grid renders twice in a real browser. NOT fixed.
- [blood-pressure] tools/blood-pressure/index.html → agent reported JSON-LD added + (likely) BP-threshold/disclaimer edits applied before failing at report-write; RE-VERIFY thresholds vs AHA and that disclaimer landed. Report file may be missing.
- Tools 41-70 whose agents failed at the final "writing the findings report" step likely HAD their HTML fixes applied (agents fix HTML first, write report last): pdf-convert, roman-numerals, car-loan, building-materials, loan-shark-compare, image-compress(done). Re-verify + write missing report files on resume.
- Tools 41-70 that failed earlier (edits likely NOT applied): ielts-calculator, land-size, visa-cost, bank-charges, plot-converter, ride-fare, fish-farming-roi, construction-budget, percentage-calc, vaccine-schedule, pharmacy-prices, clinic-costs, startup-valuation, crypto-portfolio, eg-paye, afrorates, interest-rate-ref, flyer-maker, pdf-merge-split, pdf-workspace, cac-checker, backup-power-costs. RE-RUN on resume.

### Update 2026-07-14 (main loop, post-rate-limit)
- [nda-generator] FIXED: removed the build-injected `<script src=related-tools>` from inside the printNDA `document.write` string (its `</script>` was prematurely closing the outer script and breaking print). Scripts now balanced 9/9.
  - ROOT CAUSE (shared, still to fix): the related-tools/internal-link injector matched `</body></html>` INSIDE a JS string. Harden the injector (scripts that inject before `</body>`) to skip matches inside `<script>` string literals — otherwise this regresses on next build and may affect other tools that build print/popup HTML via document.write.
- [greenhouse-cost-estimator] FIXED: added a guard so the client-side country-hub rebuild is skipped when the server-rendered `.region-section` cards already exist (was rendering every country twice). Inline JS compiles clean.


## engines/minimum-wage-engine.js — wage figure staleness (from minimum-wage-legal, 2026-07-14)

Deferred (shared engine + hardcoded hero, data verification, not changed by tool agent):
- [minimum-wage-legal] `engines/minimum-wage-engine.js` per-country rates AND `tools/minimum-wage/index.html` hero badges → VERIFY current wage floors against official gazettes. Evidence of drift: hero badges are hardcoded (`🇰🇪 KES 16,114`, `🇳🇬 ₦70,000`, `🇬🇭 GHS 18.15/day`, lines ~346) and a runtime script (lines ~1523-1531) hand-patches `R27.58/hr → R30.23/hr` at load — a symptom that hardcoded figures go stale without a data pipeline. Kenya's 2024/25 gazetted increase means `KES 16,114` is likely outdated. Refresh the engine's country rates + effective dates from primary sources, sync the hero badges, and remove the ZA runtime-patch hack. → point-in-time wage claims risk being stale for Jul 2026 payroll/legal use.
- Product backlog: the 54-country table is client-rendered ("Loading…" placeholder), so wage data is not crawlable and cannot carry Dataset/ItemList JSON-LD. Server-render the table (or inject the dataset after render) for indexable data + rich results.

- [harvest-date-estimator] `assets/js/pages/english-df-app-upgrades.js` → the df widget on `agriculture/harvest-date/index.html` (data-df-form="harvest-date-estimator") has NO handler, so submitting only echoes the static base string. Add a `harvest-date-estimator` branch that actually computes the harvest date: parse `plantingDate`, add `maturityDays` days (Date arithmetic already handles leap years — verified), and render "Expected harvest ~<toDateString>" plus the weather-risk/buyer/labour notes. WHY: the page's core promised value (planting date + days-to-maturity → harvest date) is not delivered; the visible result is a placeholder. df block left unedited per per-tool task rules.

- [car-import-cost] `df` generator (df-faq + df-upgrade blocks in tools/car-import-cost/index.html) → the auto-generated FAQ/upgrade copy is transport-fare boilerplate ("enter your route, distance and costs", "confirm current fuel prices, fares") which is wrong for a car-import tool; the FAQPage JSON-LD mirrors it, so schema is valid but off-topic. Regenerate this tool's df copy from car-import context (customs/freight/valuation/FX), not the generic fare template → improves topical relevance + GEO. Left untouched per don't-touch-df rule.
- [car-import-cost] df-form destination-currency select in tools/car-import-cost/index.html offers ZAR (South Africa) but there is no ZA rule pack under data/trade/car-import-cost-*.json and the engine has no ZA countryRulePack → the manual df planner implies a market the rule-pack engine does not cover. Reconcile in df/data layer (drop ZAR or add a labelled ZA pack). No duty-rate accuracy defect found in the six existing packs.

- [poultry-roi-calculator] `assets/js/pages/english-df-app-upgrades.js` → the `poultry-roi-calculator` df-form has NO compute branch in the switch/if-chain, so it falls through to the generic fallback that only echoes inputs ("Inputs reviewed: Birds: 500; Feed cost: ...; Sale price per bird: ...; Mortality percent: ..."). The visible output text ("revenue, feed cost, mortality, and cycle margin") is static — the widget does NO real ROI/revenue/margin math. Add a `"poultry-roi-calculator"===o` branch computing: surviving birds = birds*(1-mortality/100); revenue = surviving*salePrice; grossProfit = revenue - feedCost; ROI% = grossProfit/feedCost*100; break-even sale price = feedCost/surviving. Mirror the pattern used by `gym-roi-business`/`crop-insurance`. (Shared file + df block are off-limits to per-tool agent.)

### Update 2026-07-14 (main loop) — DEAD TOOLS revived (7) + injector root causes
Site-wide inline-JS syntax scan found **7 completely dead tools** (a build injector inserted a `<script>`/`<link>` tag — with a raw newline — INSIDE a `document.write`/`fullHTML +=` single-quoted string literal; the raw newline + raw `</script>` made the whole inline script a SyntaxError, so the tool did nothing). ALL FIXED directly:
- african-meal-plan, board-resolution, foi-template, partnership-agreement, rebar-calculator, shareholder-agreement (related-tools.min.js injected inside document.write) — line rejoined.
- creator-brand/app.html (top-level-page-ui-refresh.css `<link>` injected inside the `fullHTML` export string) — injected link removed.
- ROOT CAUSE 1 FIXED: scripts/inject-internal-links.js `insertBeforeFirst()` used indexOf('</body>') (first match) → matched `</body>` inside document.write strings. Changed to lastIndexOf for the </body> anchor.
- ROOT CAUSE 2 (still to fix): the injector that adds `/assets/css/top-level-page-ui-refresh.css` matches a `<link rel="stylesheet">` / `</head>` inside JS string literals too — harden it the same way (skip matches inside <script> blocks / use last real </head>). Until fixed, creator-brand-style export functions can regress on rebuild.
- Detector saved at scratchpad/find-broken-inline-js.js — re-run after any build to catch regressions (ignore the 6 `type="module"` false positives: ajo-tracker, boq-builder, business-plan, contract-generator, cover-letter-generator, meeting-minutes).
- [property-tax] Compare tab applies local-currency tax thresholds (e.g. ZA ZAR transfer brackets) to the USD-entered value, understating ZA transfer % for a $100k input. Rates are inline + undated ("2025/2026" badge) with no live-source freshness; property/transfer/CGT rates change with annual budgets. Kenya simplified-rental upper bound (KES 15M) not modelled. Shared rate data — not edited in the tool.

- [age-calculator] no shared-file change needed → date-math negative-days fix, applicationCategory→UtilitiesApplication, FAQ 3→5 mirrored, a11y (label for/id, aria-live) all stayed within `tools/age-calculator/index.html`.

- [color-picker] no shared-file change needed → all fixes (unique keyword H1, 12 descriptive aria-labels, conversion verification, JSON-LD/FAQ validation) stayed within `tools/color-picker/index.html`.

- [medical-report] `netlify/functions/ai-advisor.js` → shared AI infra, NOT edited. Guardrail concern (defense-in-depth): the server DOES enforce strong non-diagnostic medical guardrails, but ONLY inside the `isMedical = tool === "medical-report"` branch (line ~599: no diagnosis/prescription/medication-change/treatment, no "you are healthy", flag critical values, always append educational-not-medical-advice reminder). The tool's **AI interpretation** path (`fetchAIInterpretation`) sends `tool:'medical-report'`, so it gets those server guardrails. BUT the tool's **follow-up chat** path (`askChat`) posts `{ system, messages }` with NO `tool` field → server sees `tool` undefined → `isSiteAssistant` true → it treats the client-supplied `system` as *untrusted page context*, so the server-side medical guardrails at line 599 are NOT applied to chat. Chat safety therefore rests solely on the client `sys` string, which a crafted request could omit. FIX OPTIONS: (a) have `askChat` send `tool:'medical-report'` and move the parsed lab values into the `context` field (so server builds the medical prompt + guardrails without losing the lab data currently embedded in `sys`); or (b) server-side: apply the medical guardrail block whenever the request targets the medical tool regardless of the `messages`/`system` shape. Left for a backend owner because it changes the AI request contract and cannot be smoke-tested from the tool agent. Health-critical — this is the diagnosis/treatment refusal boundary for the chat surface.

- [remittance-v2] `tools/remittance-v2/index.html` FX consts (`RATES`/`FROM_RATES`, "updated Mar 2026") are tool-local, NOT shared — left in place, now covered by an always-visible indicative/as-of disclaimer. DEFERRED shared-infra opportunity: wire live FX via `/api/forex?base=USD` (fallback `/data/forex/latest.json`) to replace the hardcoded USD-base rates so recipient-gets/rate figures stop drifting (NGN 1535 likely stale for Jul 2026). Provider fee/margin tables remain intentionally static estimates.
