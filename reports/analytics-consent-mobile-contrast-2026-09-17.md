# Shared consent panel contrast — 2026-09-17

Isolated follow-up to `b11670cf1559040b12b09471d861860940b71710`. No coordinator writes, generated pages, consent behavior changes or deployment.

## Reproduction and source boundary

On the EN/FR/SW PDF form-filler routes at 320×740, the shared consent owner (`assets/js/components/analytics-consent-v2.js`) supplies message foreground `rgb(229,238,248)` and panel background `rgb(16,32,51)`. EN/FR measured **14.0366:1**. SW measured **1.1721:1** because earlier broad `!important` selectors in `assets/css/sw-document-pdf-a11y.css` forced a white background on the nested message container. Baseline browser results: EN and FR pass, SW fails contrast.

The fix adds `#afro-cookie-consent` to 17 existing shell-exclusion selectors in the source-owned SW Document/PDF stylesheet, consistent with its later selectors. This restores the shared consent component's own appearance instead of changing its design or escalating specificity. No declaration values, localization strings, storage logic or event names changed. Category controls remain covered by the original rules.

## Proof

After repair, all three locales show the same computed contrasts:

| Element | Foreground/background | Ratio |
|---|---|---:|
| Message | #e5eef8 / #102033 | 14.04:1 |
| Accept | #ffffff / #0062cc | 5.80:1 |
| Reject | #ffffff / #102033 | 16.45:1 |
| Privacy link | #bfdbfe / #102033 | 11.58:1 |

`NODE_PATH=C:/Users/Oza/.codex/worktrees/language-director-20260915/afrotools/node_modules`, `PORT=4416`, `CI=1`, default analytics:

`node C:/Users/Oza/.codex/worktrees/language-director-20260915/afrotools/node_modules/@playwright/test/cli.js test tests/e2e/analytics-consent-mobile-contrast.spec.js tests/e2e/analytics-consent.spec.js --grep 'consent contrast|GA4 uses|delayed core' --project=chromium --workers=1`

**5 PASS**. New route tests compute contrast from actual rendered styles/opaque ancestor background, save matched source rules, verify all controls stay within the viewport, minimum 44px control height, Tab focus with visible outline, keyboard rejection, exact `afrotools:cookie-consent` status event, persisted storage and hidden panel after reload. Existing tests retain denied-by-default/granted/rejected analytics semantics and the single-banner contract. No consent pre-seeding or bypass.

`git diff --check` passes. Screenshot visually reviewed: native SW panel text and buttons are readable within the 320px viewport.

Private before/after PNGs and computed JSON:
`C:/Users/Oza/.codex/worktrees/pdf-image-parity-20260916/evidence/consent-contrast/`

Reviewed after image:
`C:/Users/Oza/.codex/worktrees/pdf-image-parity-20260916/evidence/consent-contrast/after/consent-sw.png`

## Limits

This is actual local rendered proof for three representative routes at 320px, plus existing consent behavior tests. It is not whole-site accessibility, every theme/viewport, every overlay combination, production or provider proof. No full build was run; the CSS source is directly served and not generated by the localization owner.

## Coordinator verification
Integratedd821a550 as298e6224 on4a2c6270. Root actualsource browser session11578 passed5/5 in8.3seconds onport4518/defaultanalytics/freshserver. Three mobile computedcontrast/keyboard/persistence cases plus existing consentmode/singlebanner tests pass. No runtimebehavior or eventname edits. No rebuilt-dist or production claim.
