# Qualified public claim review — 11 September 2026

Reviewed source baseline: `a2466009` in an isolated worktree. This renews thirteen existing qualified capability/data-flow statements. It does not refresh scholarship records, rates, statutory facts, provider credentials, or source-review dates. Product wording, translations, detection rules and prohibited absolutes are unchanged. Evidence file hashes are recorded in `reports/public-claim-review-2026-09-11.json`.

The registry's `lastVerifiedAt` means the date this claim policy and its cited implementation were reviewed. It is not a declaration that every matching page, third-party provider, account operation, or workflow has passed a production end-to-end test. The next review is 11 October for stable implementation policies; the two claims supported by live storage metadata have a shorter 18 September review deadline.

Carried product debt: `scholarship-platform.js` assigns “Limited live feed” whenever the catalog is limited, including fallback mode. Its mode/degraded fields preserve the fallback distinction, but the label still needs a source-owned correction. This review does not clear that defect or attest that every public occurrence of “live” is accurate. The renewed freshness meaning is an approval requirement for wording, not a universal implementation guarantee.

| Claim | Evidence inspected and conclusion | What was not established |
| --- | --- | --- |
| `count.scholarships` | `scholarship-platform.js` derives count from response rows or the supplied public total and returns mode, timestamp, limited/degraded flags and a count-safe label. Metadata contract and static count audit pass. Renew the statement about feed totals and coverage qualifiers. | No live scholarship count, completeness, eligibility or deadline was verified. The legacy limited-feed label can contain “live” even for limited fallback data; mode/degraded flags remain essential and the registry does not approve calling fallback data live. |
| `pro.current-capabilities` | Readiness registry and PRO-APP-READINESS distinguish Payroll active from shell/local-preview apps. Billing, team and vault surfaces have explicit limitations. All six `pro:verify` owner scripts pass. | “21 routes ready” in the architecture check means routes exist; it does not mean 21 fully operational account-backed apps. No paid account/provider transaction was exercised. |
| `privacy.browser-local` | Storage helper uses browser localStorage with failed-storage fallback. PDF workflow processes bytes with PDFLib and downloads object URLs. Named network actions remain excluded from the local claim. Shared export privacy contract passes. | No universal claim that the whole site sends nothing; no audit of every matching page or browser extension. |
| `ai.optional-provider` | ai-advisor requires accepted consent and returns unavailable when no provider is configured. Shared provider boundary and AI-consent server tests retain deterministic/local alternatives and reject missing consent. | No model availability or live provider response was tested. Coordinator-owned JAMB provider changes are outside this baseline and require their own integration tests. |
| `freshness.live-data` | `api-data-freshness.js:162` requires a timestamp and source, applies feature age thresholds and takes the more cautious reported state. Shared status component includes cached/static/stale/unavailable states. Source-confidence tests verify conservative degradation. | No new successful upstream observations were obtained. A status renderer cannot certify all upstream data or all callers. |
| `authority.evidence-scope` | Source registry/schema and source-confidence model scope confidence to named sources, countries, periods and routes; unknown metadata remains cautious. Source-confidence contract passes. | No endorsement, official platform status, outcome guarantee or new source verification. |
| `statutory.jurisdiction-period` | Source metadata has jurisdiction, effective period, review dates and planning disclaimers. Source-confidence tests verify PAYE/VAT coverage and server source stamps. The salary workflow verifier is a workflow contract, not statutory authority. | No tax rate or legal fact was re-researched or renewed. Existing source dates remain untouched. |
| `account.optional-sync` | Client calls the workspace API separately from local storage. API resolves authenticated user and scopes list/update/delete records to that user. Live metadata confirms `workspace_items` RLS and authenticated own-user policies. | No account payload was read, written or synchronized; successful cross-device operation remains conditional. |
| `payment.provider-processing` | Payment client requests subscription checkout then redirects to provider authorization URL; billing reads provider subscription status; webhook checks provider signature. Billing cancellation returns 202 manualReview and does not promise immediate cancellation. | No charge, refund, cancellation, provider configuration or renewal was executed. |
| `analytics.consent-metadata` | Analytics helper gates sends/queue flushes on accepted consent. Loader filters sensitive named fields, removes raw search terms in favor of lengths and sanitizes URLs. Analytics-consent tests pass. | This is the approved metadata boundary, not a proof that arbitrary caller fields can never contain sensitive text. Provider retention, request metadata and whole-site integration remain separate. |
| `documents.local-processing` | PDF workflow loads PDF bytes with PDFLib and exports via object URLs. PDF TTS explicitly calls a provider; vault is a distinct upload action. PDF AI and shared export privacy tests pass. | No AI/TTS/email delivery was made; no blanket “never uploaded” or universal encryption claim is renewed. |
| `vault.explicit-cloud-upload` | Client requires a signed-in user and explicitly calls upload. Live `vault` bucket is private, limited to 10 MiB and PDF/PNG/JPEG; storage SELECT/INSERT policies require authenticated user's first path segment. `vault_documents` RLS is enabled with own-user policies. | No file upload/download/delete was performed; no guarantee of availability, indefinite retention or deletion. The inspected storage policies establish SELECT/INSERT only, not object DELETE. |
| `forms.email-handling` | Lead handler validates submitted email, stores via configured marketing Supabase service and separately records lifecycle delivery outcome. B2B handler requires explicit consent. Existing privacy wording describes provider processing. | No lead/contact rows were read or created, no message was sent, and unsubscribe/deletion/provider timing was not tested. |

## Live metadata evidence

Used configured `supabase_afrotools` MCP first. `get_project_url` verified project ref `zpclagtgczsygrgztlts` before SQL. One read-only metadata query inspected only pg_class, pg_policies and the named storage bucket configuration. No customer records, storage objects, tokens or keys were fetched. No live writes occurred.

Observed: RLS enabled (not FORCE RLS) for `workspace_items` and `vault_documents`; authenticated own-user SELECT/INSERT/UPDATE/DELETE policies on both; private `vault` bucket with 10,485,760-byte maximum and PDF/PNG/JPEG MIME allowlist; authenticated own-prefix SELECT/INSERT storage policies. This supports the conditional authenticated/private storage statements, not a complete security penetration test or owner/service-role restriction claim.

## Validation

Passed: scholarship API metadata test, scholarship truth audit, AI consent server test, analytics consent test, source-confidence test, all six Pro verification scripts, PDF Workspace AI assistance privacy contract, shared export privacy contract. Final claim registry/platform/audit checks are recorded in the companion JSON after the review update.

No public claim copy or translation was broadened. Source datasets, live rows, generated public pages and JAMB product files were not changed. Full release build/security/dist and production verification remain coordinator responsibilities.
