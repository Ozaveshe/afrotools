# Swahili Agriculture + Trade candidate receipt — 2026-08-08

Baseline: `6edacda8437e1fa9b9e5a512138cbdd3169e38be`. Exact denominator: **36** (Agriculture 20 + Trade 16). Candidate result: **36 accepted, 0 blocked**. This receipt does not mutate the coordinator acceptance ledger.

All 20 Agriculture apps are generated by `scripts/build-sw-agriculture-assigned-apps.js` and execute their existing English DOM-free engines through `assets/js/pages/sw-agriculture-assigned-parity.js`. The UI exposes the scenario as named JSON fields so assumptions, units, currencies and identifiers remain auditable. This is native local execution, not an iframe, redirect, transplant or network bridge.

Five regional Trade candidates are generated by `scripts/build-sw-trade-regional-parity.js`. The remaining eleven are generated by `scripts/build-sw-trade-core-parity.js` and execute the maintained HS, shipping, FX, LC, export-document, COO, demurrage, Incoterms, trade-finance and payment engines, plus the exact AfCFTA dataset. The generalized inline controller pages and the former payment-family identity ambiguity are removed from candidate ownership.

Browser proof used one worker on isolated port 44931 with installed Chrome. Every app calculated, cleared invalid/reset state, downloaded PDF/CSV/JSON/TXT, parsed JSON/CSV/TXT, reopened the PDF signature, and imported its JSON. All 36 passed 320 and 375 pixels plus 200% reflow; the eleven core Trade apps additionally passed the full matrix in light and dark modes. Console and page-error collections were empty and the synthetic raw COO marker made no network request.

Source and correctness decisions:

- No rates, official decisions or live claims were invented. Data-backed results retain the existing English datasets and engines.
- ECOWAS/EAC source-registry age is visible and the results are labeled planning scenarios requiring authority verification.
- Agriculture pesticide output retains the label-verification warning; crop insurance, finance and market outputs remain estimates.
- Accepted runtimes make no raw-input network call and require no AI. Synthetic scenarios only.
- Dedicated artwork exists for all 36 accepted candidates. Missing-artwork queue is empty.
- Reciprocal metadata touched outside Swahili: `/tools/payment-comparator/` and `/fr/tools/comparateur-paiements/` received the exact reciprocal Swahili alternate; stale competing alternates were removed from `/tools/b2b-payment/` and `/fr/tools/paiement-b2b-transfrontalier/`. No other locale UI or copy changed. `.claude/rules/i18n.md` was absent on the verified baseline and is recorded as a missing mandatory reference.

Focused proof commands:

- `node scripts/build-sw-agriculture-assigned-apps.js --check`
- `node tests/sw-agriculture-assigned-static.test.js`
- `node scripts/build-sw-trade-regional-parity.js --check`
- `node scripts/build-sw-trade-core-parity.js --check`
- `node tests/sw-trade-core-oracle.test.js`
- `node scripts/build-sw-trade-assigned-parity.js --check`
- `node tests/sw-trade-remaining-static.test.js`
- `playwright test tests/e2e/sw-agriculture-assigned-parity.spec.js --config=playwright.sw-agriculture.config.js --workers=1`
- `playwright test tests/e2e/sw-trade-assigned-regional.spec.js --config=playwright.sw-agriculture.config.js --workers=1`
- `playwright test tests/e2e/sw-trade-core-parity.spec.js --config=playwright.sw-agriculture.config.js --workers=1`

Broader validation: `validate:hreflang`, `check-links`, `audit`, `lint`, `type-check`, the focused privacy/AI server test and its three Chrome tests all passed. `build:i18n:validate` reached only `[LOCALIZATION_ARTIFACT_STALE]` for coordinator-owned locale coverage outputs; those prohibited generated artifacts were intentionally not rewritten. The audit's two missing pages (`job-offer-evaluator` and `zana-tathmini-ya-ofa-ya-kazi-sw-wave8`) are carried baseline debt, not lane regressions.
