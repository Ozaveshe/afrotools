# Crypto-remittance parity review — 2026-09-16

Base718a4795; fetched origin/main0656bb1053a25b45a538cd65b1d92edff86423b9. Isolated tree crypto-remittance-parity-20260916. Routes: /crypto/remittance/, /fr/crypto/remittance/, /sw/zana/ulinganisho-nukuu-za-kutuma-fedha/.

Shared runtime assets/js/pages/remittance-quote-comparator.js owns calculation, controls and local exports. EN/FR pages are authored; SW page is generated by scripts/build-sw-crypto-remittance.js. This is user-entered quote arithmetic; no provider quote feed, transfer execution or current fee verification.

All3 baseline browser cases ranked synthetic Senegal and Côte d’Ivoire routes together because equal USD/XOF/debit alone supplied the comparison key. The controller now enables the existing confirmed-country contract with native country fields, explicit non-equivalent-service notice and field-specific focus/errors. Copy/CSV/JSON/PDF recalculate expiry immediately before export. Invalid results and reset clear old output. Added native reset control, absent from original EN/SW UI. Copy reports success/rejection/missing API honestly and ignores stale pending completion.

CSV labels/states/payout values are native while user text remains unchanged. JSON retains the actual engine result including requireCorridor for exact replay. The French finance export wrapper had replaced native JSON with a generic DOM snapshot; only crypto-remittance opts out after native copy/CSV/JSON/PDF validation. Other wrappers remain unchanged.

PDF uses existing licensed Noto Sans asset, glyph preflight and page-boundary wrapping. Unknown glyphs block the PDF with native JSON/CSV guidance rather than silently corrupting text. Three-quote fixtures fit one page; no multipage claim. PDF.js rendered all pages and checked every text item inside bounds; pdf-parse retained Ọlá Déborah and native text. French raster was visually inspected. Files are in sibling crypto-pdf-rendered locale folders.

Nine combined core browser cases passed26.4s, including3corridor,3native export/state and3PDF fidelity cases. Original engine unit test and syntax/diff checks passed. Updated historical EN/FR/SW fixtures for required country fields; those suites not yet rerun.

Remaining findings: mobile baseline6lightPASS/6darkFAIL across320/390 (contrast); PDF visual inspection revealed CLDR obsolete ZR region alias overwriting Congo-Kinshasa name while CD code remainsCD. This shared normalizer defect requires separate immediate correction. Ordinary analytics consent-mode policy also needs separate scoped privacy evaluation. No whole-app acceptance, release build or deployment.

## Canonical region alias follow-up

PDF visual review showed Congo-Kinshasa incorrectly normalized to deprecated ZR. Intl.DisplayNames can give a modern name for an obsolete code, and the old loop overwrote the current CD name mapping. The shared engine now skips codes whose Intl.Locale canonical region differs, while retaining the explicit UK→GB alias. ZR is rejected, CD and Congo-Kinshasa agree, and CD versus CG remains non-comparable. Intl names establish identity only, never provider eligibility.

Independent node assertions cover those cases and UK/GB; baseline arithmetic still passes. Twelve browser cases across both remittance families passed29.9s, including PDF assertions for CD and no ZR. Quality digest/native fingerprint review follows separately.
