# PDF signing bounded parity repair

Base frozen b027ff71; fetched origin/main bfca5b2763fc0dc12b267b152f738eb606752e47. No coordinator edits, no deployment.

## Confirmed defects
- Guest EN/FR downloaded signed PDF; SW account gate blocked the same workflow.
- All Pages controls had no handler. EN/FR actual current/all PDFs both signed only selected page2. The generator page inherited this in SW too.

## Durable repair
`pdf-sign.patch` changes only the pdf-sign row to localFirstDownloads:true in the Swahili owner and adds signing-specific placement binding to existing shared document-pdf-tool-focus.js loaded by all3routes. Binding requires signing canvas, overlay, download button and exact current/all buttons; initializes aria-pressed and avoids duplicate handlers. It changes the existing active-button state read by the export engine. Labels and engines remain unchanged. This JS is authored (not a minify pair).

SW output was generated by actual frozen owner with scoped --write --apps=pdf-sign in a virtual filesystem backed by git show; owner validation1/1 passed. Captured generated HTML included for comparison only; root should regenerate with owner after integrating rows from other agents. No hand-edited translated output is in the patch.

## Evidence
Actual3locale mobile390 browser regression against frozen git-show server plus generatedSW/sharedJS candidate. Current-page signing selects landscape page2 (800x600); portrait page1 (600x800) stays unsigned. All-pages output has exactly1 signature on each page. Original text, dimensions, signature coordinates and physical bounds asserted in parsed actual PDFs. Six downloaded PDFs/12pages rendered by Poppler and visually reviewed in proof/contact.png. No uploads observed; save-signature opt-in remains off and actual localStorage key stays empty. Fresh cookie decline uses normal button. Typed synthetic ASCII signature only.

## Limits
Not full signing-app acceptance: drawn/uploaded signatures, drag/resize, arbitrary Unicode typed names/font fidelity, rotated CropBoxes, date placement and huge PDFs remain unverified. Shared source presently draws typed text with fixed standard font/size despite preview font selection; no fidelity claim made. Current all-page placement uses same point offsets on different page sizes rather than proportional position. No source mutation to these behaviors. This is a visual signature, not cryptographic signature verification or legal acceptance evidence.

Run repo-native regression after integration/regeneration: PLAYWRIGHT_BASE_URL=<server> node node_modules/@playwright/test/cli.js test tests/e2e/pdf-sign-guest-parity.spec.js --workers=1. Test does not intercept candidate assets. Full source hashes and actual proof hashes are in proof.json.

Final native regression:3/3PASS15.6s (session13525), candidate server4513. Earlier geometry/visual run3/3PASS20.3s; its six artifact bytes are preserved and hashed. Final run additionally checks correct actual signature-storage key. git apply --check --ignore-space-change against coordinator PASS. Own tree clean; source patch only, no commit needed for private frozen overlay.

## Coordinator validation
Integrated scoped source/test patch on f57cda95 and regenerated only SWsign via owner; targeted ownercheck1/1passed. Sharedsource syntax anddiffcheckpassed. Root browser session38955 passed3/3 in20.6seconds against actual integrated source on port4510, defaultanalytics/freshserver, including parsed mixed-size current/all exportedPDFs. No dist or production claim. Sourcepatch did not overwrite the previously integrated form-filler generator row.
