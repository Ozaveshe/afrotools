(function harvestDateEngineModule(root,factory){'use strict';var api=factory();if(typeof module==='object'&&module.exports)module.exports=api;if(root){root.AfroTools=root.AfroTools||{};root.AfroTools.HarvestDateEngine=api;}}(typeof window!=='undefined'?window:globalThis,function createHarvestDateEngine(){'use strict';
function dateParts(value){var match=/^(\d{4})-(\d{2})-(\d{2})$/.exec(String(value||''));if(!match)return null;var year=Number(match[1]),month=Number(match[2]),day=Number(match[3]),date=new Date(Date.UTC(year,month-1,day));if(date.getUTCFullYear()!==year||date.getUTCMonth()!==month-1||date.getUTCDate()!==day)return null;return{year:year,month:month,day:day};}
function isoDate(date){return date.getUTCFullYear()+'-'+String(date.getUTCMonth()+1).padStart(2,'0')+'-'+String(date.getUTCDate()).padStart(2,'0');}
function calculate(input){var planting=dateParts(input&&input.plantingDate),maturityDays=Number(input&&input.maturityDays),crop=String(input&&input.crop||''),weatherRisk=String(input&&input.weatherRisk||'');if(!planting||!Number.isFinite(maturityDays)||maturityDays<0||!crop||!weatherRisk)return{ok:false,status:'invalid-input'};var harvest=new Date(Date.UTC(planting.year,planting.month-1,planting.day));harvest.setUTCDate(harvest.getUTCDate()+maturityDays);return{ok:true,status:'calculated',input:{plantingDate:input.plantingDate,crop:crop,maturityDays:maturityDays,weatherRisk:weatherRisk},harvestDate:isoDate(harvest)};}
return{dateParts:dateParts,calculate:calculate};}));
