(function(root,factory){var api=factory();if(typeof module==="object"&&module.exports)module.exports=api;if(root)root.DentalQuoteEngine=api;})(typeof window!=="undefined"?window:this,function(){"use strict";function n(v,l,max,int){var x=Number(v);if(!Number.isFinite(x)||x<0||x>max||(int&&(!Number.isInteger(x)||x<1)))throw new Error(l+" is outside the allowed range.");return x;}function req(i,k,l,max){var s=String(i&&i[k]||"").trim();if(!s)throw new Error("Enter "+l+".");return s.slice(0,max);}function realDate(v){var s=String(v||""),m=s.match(/^(\d{4})-(\d{2})-(\d{2})$/);if(!m)throw new Error("Enter the quote date.");var d=new Date(Date.UTC(Number(m[1]),Number(m[2])-1,Number(m[3])));if(d.getUTCFullYear()!==Number(m[1])||d.getUTCMonth()!==Number(m[2])-1||d.getUTCDate()!==Number(m[3]))throw new Error("Enter a real calendar quote date.");return s;}function calculate(i){var currency=String(i.currency||"").trim().toUpperCase(),date=realDate(i.quoteDate);if(!/^[A-Z]{3}$/.test(currency))throw new Error("Enter a three-letter currency code.");var qty=n(i.quantity,"Quantity",100,true),unit=n(i.unitPrice,"Unit price",1e10),subtotal=qty*unit,gross=subtotal+["consultation","imaging","followup","medicines","travel"].reduce(function(s,k){return s+n(i[k],k,1e10);},0),insurance=n(i.insuranceContribution,"Insurance contribution",1e10),buffer=n(i.bufferPercent,"Buffer",100);if(insurance>gross)throw new Error("Insurance contribution cannot exceed quoted gross.");var oop=gross-insurance,buf=oop*buffer/100;return{provider:req(i,"provider","provider/reference",100),service:req(i,"service","provider-described service",120),currency:currency,quoteDate:date,quantity:qty,unitPrice:unit,bufferPercent:buffer,serviceSubtotal:Math.round(subtotal*100)/100,gross:Math.round(gross*100)/100,insuranceContribution:insurance,bufferAmount:Math.round(buf*100)/100,totalWithBuffer:Math.round((oop+buf)*100)/100};}return{calculate:calculate};});
