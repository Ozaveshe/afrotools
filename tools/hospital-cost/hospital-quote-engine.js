(function(root,factory){var api=factory();if(typeof module==="object"&&module.exports)module.exports=api;if(root)root.HospitalQuoteEngine=api;})(typeof window!=="undefined"?window:this,function(){"use strict";function n(v,l,max){var x=Number(v);if(!Number.isFinite(x)||x<0||x>max)throw new Error(l+" must be between 0 and "+max+".");return x;}function validDate(value){if(!/^\d{4}-\d{2}-\d{2}$/.test(value))return false;var parsed=new Date(value+"T00:00:00Z");return!Number.isNaN(parsed.getTime())&&parsed.toISOString().slice(0,10)===value&&value<=new Date().toISOString().slice(0,10);}function calculate(i){var facility=String(i&&i.facility||"").trim(),currency=String(i.currency||"").trim().toUpperCase(),date=String(i.quoteDate||"");if(!facility)throw new Error("Enter a facility or quote reference.");if(!/^[A-Z]{3}$/.test(currency))throw new Error("Enter a three-letter currency code.");if(!validDate(date))throw new Error("Enter a valid quote date that is not in the future.");var keys=["consultation","facilityFee","procedure","tests","medicines","travel","other"],gross=keys.reduce(function(s,k){return s+n(i[k],k,1e10);},0),insurance=n(i.insuranceContribution,"Insurance contribution",1e10),buffer=n(i.bufferPercent,"Buffer percent",100);if(insurance>gross)throw new Error("Confirmed insurer contribution cannot exceed quoted gross.");if(gross<=0)throw new Error("Enter at least one positive provider-confirmed quote line.");var oop=gross-insurance,buf=oop*buffer/100;return{facility:facility.slice(0,100),currency:currency,quoteDate:date,bufferPercent:buffer,gross:Math.round(gross*100)/100,insuranceContribution:insurance,outOfPocket:Math.round(oop*100)/100,bufferAmount:Math.round(buf*100)/100,totalWithBuffer:Math.round((oop+buf)*100)/100};}return{calculate:calculate};});
