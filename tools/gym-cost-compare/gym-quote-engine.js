(function(root,factory){var api=factory();if(typeof module==="object"&&module.exports)module.exports=api;if(root)root.GymQuoteEngine=api;})(typeof window!=="undefined"?window:this,function(){"use strict";function n(v,l,min,max,whole){var x=Number(v);if(!Number.isFinite(x)||x<min||x>max||(whole&&!Number.isInteger(x)))throw new Error(l+" must be "+(whole?"a whole number ":"")+"between "+min+" and "+max+".");return x;}function realDate(v){var s=String(v||""),m=s.match(/^(\d{4})-(\d{2})-(\d{2})$/);if(!m)throw new Error("Enter the quote date.");var d=new Date(Date.UTC(Number(m[1]),Number(m[2])-1,Number(m[3])));if(d.getUTCFullYear()!==Number(m[1])||d.getUTCMonth()!==Number(m[2])-1||d.getUTCDate()!==Number(m[3]))throw new Error("Enter a real calendar quote date.");return s;}function provider(i,p,months,visits){var name=String(i[p+"Name"]||"").trim();if(!name)throw new Error("Enter provider "+p.toUpperCase()+" name.");var monthly=n(i[p+"Monthly"],name+" monthly fee",0,1e9,false),joining=n(i[p+"Joining"],name+" joining fee",0,1e9,false),transport=n(i[p+"Transport"],name+" transport",0,1e9,false),extras=n(i[p+"Extras"],name+" other costs",0,1e9,false),total=joining+monthly*months+transport*visits*months+extras,totalVisits=visits*months;return{name:name.slice(0,80),monthly:monthly,joining:joining,transport:transport,extras:extras,total:Math.round(total*100)/100,costPerVisit:totalVisits?Math.round(total/totalVisits*100)/100:0};}function calculate(i){var currency=String(i.currency||"").trim().toUpperCase(),date=realDate(i.quoteDate);if(!/^[A-Z]{3}$/.test(currency))throw new Error("Enter a three-letter currency code.");var months=n(i.months,"Months",1,36,true),visits=n(i.visitsPerMonth,"Visits per month",0,100,true),a=provider(i,"a",months,visits),b=provider(i,"b",months,visits);return{currency:currency,quoteDate:date,months:months,visitsPerMonth:visits,a:a,b:b,difference:Math.round(Math.abs(a.total-b.total)*100)/100};}return{calculate:calculate};});
