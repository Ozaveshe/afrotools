(function(root,factory){var api=factory();if(typeof module==="object"&&module.exports)module.exports=api;if(root)root.PharmacyQuoteEngine=api;})(typeof window!=="undefined"?window:this,function(){"use strict";function req(i,k,label,max){var s=String(i&&i[k]||"").trim();if(!s)throw new Error("Enter "+label+".");return s.slice(0,max);}function n(v,l,min,max,integer){var x=Number(v);if(!Number.isFinite(x)||x<min||x>max||(integer&&!Number.isInteger(x)))throw new Error(l+" must be "+(integer?"a whole number ":"")+"between "+min+" and "+max+".");return x;}function realDate(v){var s=String(v||""),m=s.match(/^(\d{4})-(\d{2})-(\d{2})$/);if(!m)throw new Error("Enter the quote date.");var d=new Date(Date.UTC(Number(m[1]),Number(m[2])-1,Number(m[3])));if(d.getUTCFullYear()!==Number(m[1])||d.getUTCMonth()!==Number(m[2])-1||d.getUTCDate()!==Number(m[3]))throw new Error("Enter a real calendar quote date.");return s;}function calculate(i){var currency=String(i.currency||"").trim().toUpperCase(),date=realDate(i.quoteDate);if(!/^[A-Z]{3}$/.test(currency))throw new Error("Enter a three-letter currency code.");var pack=n(i.packSize,"Pack size",1,100000,true),price=n(i.packPrice,"Pack price",0,1e9),units=n(i.requiredUnits,"Required units",1,100000,true),fee=n(i.fee,"Fee",0,1e9),packs=Math.ceil(units/pack);return{medicine:req(i,"medicine","medicine label name",100),strength:req(i,"strength","label strength",60),dosageForm:req(i,"dosageForm","dosage form",60),pharmacy:req(i,"pharmacy","pharmacy or quote reference",100),currency:currency,quoteDate:date,packSize:pack,packPrice:price,requiredUnits:units,fee:fee,unitPrice:Math.round(price/pack*10000)/10000,packsNeeded:packs,unusedUnits:packs*pack-units,totalCost:Math.round((packs*price+fee)*100)/100};}return{calculate:calculate};});
