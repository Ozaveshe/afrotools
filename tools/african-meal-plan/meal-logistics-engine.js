(function(root,factory){var api=factory();if(typeof module==="object"&&module.exports)module.exports=api;if(root)root.MealLogisticsEngine=api;})(typeof window!=="undefined"?window:this,function(){"use strict";function num(v,label,min,max,whole){var n=Number(v);if(!Number.isFinite(n)||n<min||n>max||(whole&&!Number.isInteger(n)))throw new Error(label+" must be "+(whole?"a whole number ":"")+"between "+min+" and "+max+".");return n;}function date(v,label){var s=String(v||""),m=s.match(/^(\d{4})-(\d{2})-(\d{2})$/);if(!m)throw new Error("Enter "+label+".");var d=new Date(Date.UTC(Number(m[1]),Number(m[2])-1,Number(m[3])));if(d.getUTCFullYear()!==Number(m[1])||d.getUTCMonth()!==Number(m[2])-1||d.getUTCDate()!==Number(m[3]))throw new Error("Enter a real calendar date for "+label+".");return s;}function calculate(i){var currency=String(i&&i.currency||"").trim().toUpperCase();if(!/^[A-Z]{3}$/.test(currency))throw new Error("Enter a three-letter currency code.");var priceDate=date(i.priceDate,"the date prices were checked");var days=num(i.days,"Days",1,31,true),people=num(i.people,"People",1,50,true),meals=num(i.mealsPerDay,"Meals per day",1,8,true),daily=num(i.dailyBudget,"Daily budget",0,1000000000,false),buffer=num(i.bufferPercent,"Buffer",0,100,false),base=days*people*daily;return{days:days,people:people,mealsPerDay:meals,dailyBudget:daily,bufferPercent:buffer,currency:currency,priceDate:priceDate,notes:String(i.notes||"").trim().slice(0,1000),totalServings:days*people*meals,baseBudget:Math.round(base*100)/100,bufferAmount:Math.round(base*buffer)/100,totalBudget:Math.round(base*(1+buffer/100)*100)/100};}return{calculate:calculate};});
