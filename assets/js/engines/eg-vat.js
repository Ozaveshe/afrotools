(function(root,factory){var api=factory();if(typeof module==='object'&&module.exports)module.exports=api;if(root){root.AfroTools=root.AfroTools||{};root.AfroTools.EGVatEngine=api;}})(typeof globalThis!=='undefined'?globalThis:this,function(){
  'use strict';
  var STANDARD_RATE=14,REGISTRATION_THRESHOLD=500000,REVIEWED_ON='2026-07-22';
  function finite(value,fallback){var number=Number(value);return Number.isFinite(number)?number:fallback;}
  function roundMoney(value){return Math.round((finite(value,0)+Number.EPSILON)*100)/100;}
  function amount(value){var number=finite(value,NaN);if(!Number.isFinite(number)||number<0)throw new RangeError('amount must be a non-negative number');return number;}
  function rate(value){var number=finite(value,NaN);if(!Number.isFinite(number)||number<0||number>100)throw new RangeError('rate must be between 0 and 100');return number;}
  function calculate(input){input=input||{};var entered=amount(input.amount),usedRate=rate(input.rate==null?STANDARD_RATE:input.rate),mode=input.mode==='extract'?'extract':'add',net=mode==='extract'?entered/(1+usedRate/100):entered,vat=net*usedRate/100,gross=mode==='extract'?entered:net+vat;return{mode:mode,rate:usedRate,rateKind:input.rateKind==='scenario'?'scenario':(usedRate===0?'zero':'standard'),net:roundMoney(net),vat:roundMoney(vat),gross:roundMoney(gross)};}
  function calculateInvoice(lines,usedRate,rateKind){if(!Array.isArray(lines)||!lines.length)throw new RangeError('at least one invoice line is required');var normalized=lines.map(function(line){var quantity=amount(line.quantity),unitPrice=amount(line.unitPrice);return{description:String(line.description||''),quantity:quantity,unitPrice:unitPrice,net:roundMoney(quantity*unitPrice)};}),net=roundMoney(normalized.reduce(function(sum,line){return sum+line.net;},0)),result=calculate({amount:net,rate:usedRate,rateKind:rateKind,mode:'add'});result.lines=normalized;return result;}
  function classify(key){if(key==='standard')return{treatment:'standard',rate:STANDARD_RATE,source:'VAT Law No. 67 of 2016, Article 3'};if(key==='confirmed-zero')return{treatment:'zero-rated',rate:0,source:'Confirmed under the VAT Law and its attached schedules'};if(key==='confirmed-special')return{treatment:'special',rate:null,source:'Confirmed in the goods and services table attached to the VAT Law'};if(key==='confirmed-exempt')return{treatment:'exempt',rate:null,source:'Confirmed in the exemption list attached to the VAT Law'};return{treatment:'review',rate:null,source:'Confirm the exact VAT Law article, table item or exemption entry with ETA'};}
  function registrationBand(turnover){return amount(turnover)>=REGISTRATION_THRESHOLD?'threshold-review':'below-threshold';}
  return{STANDARD_RATE:STANDARD_RATE,REGISTRATION_THRESHOLD:REGISTRATION_THRESHOLD,REVIEWED_ON:REVIEWED_ON,roundMoney:roundMoney,calculate:calculate,calculateInvoice:calculateInvoice,classify:classify,registrationBand:registrationBand};
});
