(function(root,factory){var api=factory();if(typeof module==='object'&&module.exports)module.exports=api;if(root){root.AfroTools=root.AfroTools||{};root.AfroTools.MWVatEngine=api}})(typeof globalThis!=='undefined'?globalThis:this,function(){'use strict';var STANDARD_RATE=17.5;function amount(v){if(v===''||v===null||typeof v==='undefined')throw new RangeError('amount is required');var n=Number(v);if(!Number.isFinite(n)||n<0)throw new RangeError('amount must be non-negative');return n}function round(v){return Math.round((v+Number.EPSILON)*100)/100}function calculate(input){input=input||{};if(input.rateKind&&input.rateKind!=='standard'){var error=new RangeError('Malawi special VAT treatment requires current schedule evidence and is not modeled');error.code='RATE_EVIDENCE_REQUIRED';throw error}var entered=amount(input.amount),mode=input.mode==='extract'?'extract':'add',net=mode==='extract'?entered/(1+STANDARD_RATE/100):entered,gross=mode==='extract'?entered:net*(1+STANDARD_RATE/100);net=round(net);gross=round(gross);return{mode:mode,rateKind:'standard',rate:STANDARD_RATE,net:net,vat:round(gross-net),gross:gross,rounding:'nearest-tambala',sourceAsOf:'2026-07-22'}}return{STANDARD_RATE:STANDARD_RATE,REVIEWED_ON:'2026-07-22',calculate:calculate,formulaParameters:{standard:'17.5% under Act 37 of 2025',legacyConflict:'older MRA EIS examples still display 16.5%; do not use them as the current rate',specialTreatments:'not modeled without current exact schedule evidence',rounding:'nearest MWK 0.01'}}});
