(function(){"use strict";var engine=window.BoreholeCostEngine;function el(id){return document.getElementById(id)}function fmt(result,n){return result.currency+Math.round(n).toLocaleString("en")}function estimate(){if(!engine)return;var result=engine.estimate({country:el("country").value,depth:el("depth").value,geology:el("geology").value,pumpType:el("pump").value,tankSize:el("tank").value});if(result.error){el("resultCard").style.display="none";window.alert("Enter a valid country, depth, geology, pump and tank combination.");return}el("resultGrid").innerHTML=[[fmt(result,result.total),"Legacy estimate"],[result.depth+" m","Drilling depth"],[fmt(result,result.breakdown.drilling),"Drilling only"],[fmt(result,result.ratePerM)+"/m","Legacy rate"]].map(function(x){return'<div class="result-box'+(x[1]==="Legacy estimate"?' highlight':'')+'"><div class="num">'+x[0]+'</div><div class="lbl">'+x[1]+"</div></div>"}).join("");var names={survey:"Hydrogeological survey",mobilization:"Mobilization / site prep",drilling:"Drilling",casing:"Casing & lining",screen:"Screen & filter",gravel:"Gravel pack",development:"Development & test pumping",pump:"Pump",plumbing:"Plumbing & installation",tank:"Water tank",waterTest:"Water quality test"};el("breakdownTable").innerHTML="<tr><th>Item</th><th>Legacy allowance</th></tr>"+Object.keys(result.breakdown).filter(function(key){return result.breakdown[key]>0}).map(function(key){return"<tr><td>"+names[key]+"</td><td>"+fmt(result,result.breakdown[key])+"</td></tr>"}).join("")+'<tr class="total"><td>TOTAL LEGACY ESTIMATE</td><td>'+fmt(result,result.total)+"</td></tr>";el("resultCard").style.display="block";el("resultCard").scrollIntoView({behavior:"smooth"})}window.estimate=estimate})();
